{
  "client": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "FileConfig",
    "description": "The Aperio client configuration file (`aperio.yaml` or `~/.aperio.yaml`).\nEvery key is optional and can equally be set with a CLI flag or an `APERIO_*`\nenvironment variable; this file is the lowest-friction way to keep them.",
    "type": "object",
    "properties": {
      "adaptive_concurrency": {
        "description": "Lower the announced `max_concurrent` while requests queue up waiting for\na local permit, and climb back when they stop. The server already queues\nrather than dispatching past the announced number, so this is how a\nclient that has become slow stops being sent work it cannot do; the\nserver then holds the request, picks another client, or asks for capacity\nthrough autoscaling, all of which beat a refusal. Needs `max_concurrent`\nto be set, since that is the number being moved. Default: `false`\n(env: APERIO_ADAPTIVE_CONCURRENCY).",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          true
        ]
      },
      "allowed_ips": {
        "description": "Visitor IPs/CIDRs allowed to reach this service (plain IPs or CIDR\nranges); empty/unset = everyone. Enforced by the server before dispatch.",
        "type": [
          "array",
          "null"
        ],
        "examples": [
          [
            "203.0.113.7",
            "10.0.0.0/8"
          ]
        ],
        "items": {
          "type": "string"
        }
      },
      "auth": {
        "description": "Gate this client behind your own `user:password` login instead of the server's.",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "admin:s3cret"
        ]
      },
      "bandwidth": {
        "description": "Total link capacity of this client: a budget divided across every service\nand every parallel connection, so the sum the server is allowed to push\nnever exceeds it. Bit suffixes (`kbit`/`mbit`/`gbit`) count as /8, byte\nsuffixes (`kb`/`mb`/`gb`, or bare `k`/`m`/`g`) as x1000.",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "8mbit",
          "500kbit",
          "2MB"
        ]
      },
      "bind-tunnels": {
        "description": "Tunnels this process binds to local ports, keyed by the tunnel's name.\nA key naming a peer's client id instead binds every tunnel that peer\ndeclares, which is the older spelling and still works.",
        "type": [
          "object",
          "null"
        ],
        "additionalProperties": {
          "$ref": "#/$defs/BindTunnelValue"
        },
        "examples": [
          {
            "dns": {
              "port": 15353,
              "token": "apr_binder_token"
            },
            "pg_main": 15432
          }
        ]
      },
      "cache": {
        "description": "Let the server cache GET responses (per their `Cache-Control`);\neffective only when the server enables APERIO_CACHE. Default: `false`.",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          true
        ]
      },
      "capture": {
        "description": "Record transactions for the dashboard's request inspector (services may\noverride). Default: `true`.",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          false
        ]
      },
      "circuit_breaker": {
        "description": "Circuit breaker for the backend. The default for every `services:` entry\nthat does not set its own.",
        "anyOf": [
          {
            "$ref": "#/$defs/CircuitBreakerConfig"
          },
          {
            "type": "null"
          }
        ],
        "examples": [
          {
            "failures": 5,
            "open_for": 30
          }
        ]
      },
      "client_id": {
        "description": "Fixed instance UUID kept across restarts, so failover and `--bind-tunnels`\ncan recognize this client; a random one is used when unset.",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "3f2504e0-4f89-41d3-9a0c-0305e82c3301"
        ]
      },
      "connect_timeout": {
        "description": "Seconds to wait for the TCP connection to this backend before giving\nup, separate from `timeout`, which covers the whole request. A backend\nacross a VPN needs longer than one on loopback, and one number for both\nmeans either slow failure detection everywhere or spurious failures for\nthe far one. Default: unset (the whole-request `timeout` applies)\n(env: APERIO_CONNECT_TIMEOUT).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "examples": [
          2
        ],
        "minimum": 0
      },
      "connections": {
        "description": "Parallel tunnel connections opened for the exposed service (the server's\n`max_connections_per_service` is the ceiling); the server load-balances\nacross them like separate clients, so a single dropped connection leaves\nno visitor-facing gap.\nA number opens exactly that many at startup; `{min: 1, max: 8}` opens the\nfloor and grows towards the ceiling while requests queue up, then shrinks\nback when they stop. Default: `1`.",
        "anyOf": [
          {
            "$ref": "#/$defs/Connections"
          },
          {
            "type": "null"
          }
        ],
        "examples": [
          2,
          {
            "max": 8,
            "min": 1
          }
        ]
      },
      "custom_name": {
        "description": "What to call the service on screen when one is named on the command line\nor in the environment (`APERIO_CUSTOM_NAME`). A `services:` entry carries\nits own `custom_name:`, which is where a config file says it.",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "Public Web"
        ]
      },
      "denied": {
        "description": "Redirect URL for visitors rejected by `allowed_ips` when no candidate\nof the route admits them (unset = stealth; services may override).",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "https://example.com/not-for-you"
        ]
      },
      "depends_on": {
        "description": "Names of services in this same file that must have a live tunnel before\nthis one opens its own. Waits for them, then proceeds regardless after a\nbounded grace period, because a dependency that never arrives must not\nkeep a service that could serve traffic off the air forever.",
        "type": [
          "array",
          "null"
        ],
        "examples": [
          [
            "api"
          ]
        ],
        "items": {
          "type": "string"
        }
      },
      "device_key": {
        "description": "Trust-on-first-use device key announced with the tunnel token, so a\nserver with token pinning on can bind that token to this machine\n(env: APERIO_DEVICE_KEY).",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "9f8c1d2e3a4b5c6d7e8f9a0b1c2d3e4f9f8c1d2e3a4b5c6d7e8f9a0b1c2d3e4f"
        ]
      },
      "device_key_file": {
        "description": "File holding the device key; a random one is generated and persisted\nthere on first run. Ignored when `device_key` is set directly\n(env: APERIO_DEVICE_KEY_FILE).",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "/var/lib/aperio/device.key"
        ]
      },
      "headers": {
        "description": "Request/response header add-remove rules applied by this client to\nproxied HTTP traffic (services may override with their own `headers`).",
        "anyOf": [
          {
            "$ref": "#/$defs/HeaderRules"
          },
          {
            "type": "null"
          }
        ],
        "examples": [
          {
            "request": {
              "add": {
                "X-Forwarded-Env": "staging"
              },
              "remove": [
                "X-Internal-Debug"
              ]
            },
            "response": {
              "add": {
                "X-Served-By": "aperio"
              },
              "remove": [
                "X-Powered-By"
              ]
            }
          }
        ]
      },
      "health": {
        "description": "Backend health probing (`endpoint`, `interval`, `timeout`, `threshold`,\n`wait_for_backend`). Preferred over the flat `target_health` / `health_*`\nkeys, which still work; `services:` entries may override it per service.",
        "anyOf": [
          {
            "$ref": "#/$defs/TopHealthConfig"
          },
          {
            "type": "null"
          }
        ],
        "examples": [
          {
            "interval": 10,
            "threshold": 2,
            "timeout": 5
          }
        ]
      },
      "health_interval": {
        "description": "Seconds between backend health probes.",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "examples": [
          10
        ],
        "minimum": 0
      },
      "health_threshold": {
        "description": "Failed probes in a row before the backend is reported unhealthy.",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint32",
        "examples": [
          3
        ],
        "minimum": 0
      },
      "health_timeout": {
        "description": "Seconds to wait for each health probe before counting it as failed.",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "examples": [
          5
        ],
        "minimum": 0
      },
      "hostname": {
        "description": "**Not accepted in a config file since 0.9.0; CLI and environment only.** Public hostname(s)\nto claim for this client's traffic. A bind belongs to the service it\nbinds, so write it on the `services:` entry; `--hostname` and\n`APERIO_HOSTNAME` are unaffected.",
        "anyOf": [
          {
            "$ref": "#/$defs/Hostnames"
          },
          {
            "type": "null"
          }
        ],
        "deprecated": true,
        "examples": [
          "app.example.com",
          [
            "app.example.com",
            "www.example.com"
          ]
        ]
      },
      "idle_timeout": {
        "description": "Shut this client down after it has served no request for this long, e.g.\n`5m` (unset = never). The scale-in half of `scaling`: the server never\nstops anything, an idle client retires itself. The shutdown is graceful\n(the server stops routing to it first, in-flight requests finish), and\nthe timer only starts once the client has served its first request, so a\nslow cold start cannot make it exit before it is ever used.",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "5m"
        ]
      },
      "include": {
        "description": "Other config files to read before this one, each path relative to the\nfile that names it. Their keys are used unless this file sets them, and\nsequences of mappings (`services:`, `subscribe:`, `expose:`) concatenate\nwith the includes first, so a fragment adds services rather than\nreplacing them. Later includes win over earlier ones, and this file wins\nover all of them. Chains may nest up to five deep; a cycle is an error.",
        "type": [
          "array",
          "null"
        ],
        "examples": [
          [
            "services/prod.yaml",
            "shared/health.yaml"
          ]
        ],
        "items": {
          "type": "string"
        }
      },
      "ip_family": {
        "description": "IP family used to dial the tunnel server: `auto` (tries both), `ipv4`,\nor `ipv6`. Set `ipv4` when the server hostname resolves to an IPv6\naddress the host cannot reach (env: APERIO_IP_FAMILY). Default: `auto`.",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "auto",
          "ipv4"
        ]
      },
      "log_format": {
        "description": "Log output format: `json`, or `pretty`/`text` for the human-readable\nform. Unset auto-detects, a TTY gets `pretty` and a pipe gets `json`\n(env: APERIO_LOG_FORMAT).",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "json",
          "pretty"
        ]
      },
      "log_level": {
        "description": "Log verbosity of this client (env: LOG_LEVEL; `RUST_LOG` overrides\nboth). Default: `info`.",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "info",
          "debug"
        ]
      },
      "max_concurrent": {
        "description": "Most requests handled at once before the server queues the rest.",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint32",
        "examples": [
          8
        ],
        "minimum": 0
      },
      "max_message_size": {
        "description": "Largest single tunnel frame, in bytes, the client will accept.\nDefault: `33554432` (32 MB).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint",
        "examples": [
          33554432
        ],
        "minimum": 0
      },
      "max_redirects": {
        "description": "How many backend redirects to follow transparently before passing one\nthrough. Default: `5`.",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint",
        "examples": [
          5
        ],
        "minimum": 0
      },
      "max_request_body": {
        "description": "Largest request body, in bytes, visitors may upload to this service;\nthe server rejects bigger uploads with 413 before they enter the tunnel.",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "examples": [
          1048576
        ],
        "minimum": 0
      },
      "max_response_body": {
        "description": "Largest response body, in bytes, the client will relay to a visitor.\nDefault: `52428800` (50 MB).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint",
        "examples": [
          10485760
        ],
        "minimum": 0
      },
      "messages_listen": {
        "description": "Local address the message face listens on, so an application on this\nmachine can subscribe (SSE) and publish (POST) without speaking the\ntunnel protocol. Unset = no local listener.",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "127.0.0.1:1888"
        ]
      },
      "messages_mqtt_listen": {
        "description": "Local address an MQTT listener answers on, for an application that would\nrather use the MQTT client library it already has. MQTT 3.1.1, QoS 0;\nthe protocol never leaves this machine. Unset = no MQTT listener.",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "127.0.0.1:1883"
        ]
      },
      "metrics_labels": {
        "description": "Static Prometheus labels attached to this client's own metric series,\ne.g. `{env: prod, region: eu-west}`, so one Prometheus can serve several\nenvironments without relabelling rules. At most 8 labels; names must be\n`[a-zA-Z_][a-zA-Z0-9_]*` and the server drops anything else, including\nthe names it writes itself (`client_id`, `job`, `instance`, …).",
        "type": [
          "object",
          "null"
        ],
        "additionalProperties": {
          "type": "string"
        },
        "examples": [
          {
            "env": "prod",
            "region": "eu-west"
          }
        ]
      },
      "min_tls_version": {
        "description": "Lowest TLS version accepted from an `https://` backend: `1.2` or `1.3`.\nPer service because a fleet with one legacy backend should not have to\nlower the floor for all of them. Default: rustls's own floor\n(env: APERIO_MIN_TLS_VERSION).",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "1.3"
        ]
      },
      "otel_bridge": {
        "description": "Accept OpenTelemetry exports from things running next to this client and\ncarry them to the server, which forwards them to the collector it is\nconfigured for. The point is an edge host that is allowed exactly one\noutbound connection, the tunnel: no new firewall rule, no collector\ncredential at the edge.",
        "anyOf": [
          {
            "$ref": "#/$defs/OtelBridge"
          },
          {
            "type": "null"
          }
        ],
        "examples": [
          {
            "listen": "127.0.0.1:4318",
            "transport": "tunnel"
          }
        ]
      },
      "pass_hostname": {
        "description": "Forward the visitor's original Host header to the backend instead of the\ntarget's. Default: `false`.",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          false
        ]
      },
      "path": {
        "description": "**Not accepted in a config file since 0.9.0; CLI and environment only.** Public path prefix\nto claim for this client's traffic. Write it on the `services:` entry\nit binds; `--path` and `APERIO_PATH` are unaffected.",
        "type": [
          "string",
          "null"
        ],
        "deprecated": true,
        "examples": [
          "/api"
        ]
      },
      "pid_file": {
        "description": "Path to write this process's pid to at startup, removed on a clean exit.\nFor an init system that wants one; a process supervisor usually knows the\npid without being told. Default: unset (env: APERIO_PID_FILE).",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "/run/aperio-client.pid"
        ]
      },
      "priority": {
        "description": "Failover tier for this client (0 = primary, higher numbers are standbys).\nDefault: `0`.",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint32",
        "examples": [
          0
        ],
        "minimum": 0
      },
      "public": {
        "description": "Serve without the server's visitor login (needs a token that allows it).\nDefault: `false`.",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          true
        ]
      },
      "reload_drain": {
        "description": "Seconds to let in-flight requests finish when a configuration reload\nstops a service, before its tunnel connection is dropped. The client\nannounces `Draining` first, so the server sends it nothing new while it\nfinishes. `0` drops the connection immediately, which is what happened\nbefore this existed. Default: `10`\n(env: APERIO_RELOAD_DRAIN).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "examples": [
          10
        ],
        "minimum": 0
      },
      "resilience": {
        "description": "Keep serving this service's cached responses (marked, even past their\nlifetime) while no healthy client is connected, instead of failing with\n504 (needs `cache: true` and the server-side cache enabled).\nDefault: `false`.",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          true
        ]
      },
      "response_timeout": {
        "description": "Seconds the server should wait for this service to answer a dispatched\nrequest before failing it, a per-service override of the server's global\ngateway response timeout (defaults applied per service).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "examples": [
          120
        ],
        "minimum": 0
      },
      "retry": {
        "description": "Retry policy for backend requests that fail before a response arrives.\nThe default for every `services:` entry that does not set its own.",
        "anyOf": [
          {
            "$ref": "#/$defs/RetryConfig"
          },
          {
            "type": "null"
          }
        ],
        "examples": [
          {
            "attempts": 3,
            "backoff": 100
          }
        ]
      },
      "scaling": {
        "description": "Autoscaling: the endpoint the server calls when this client's services\nneed capacity. Applies to every service this client exposes; each\nhostname bind gets its own record on the server.",
        "anyOf": [
          {
            "$ref": "#/$defs/ScalingDecl"
          },
          {
            "type": "null"
          }
        ],
        "examples": [
          {
            "cold_start": "45s",
            "max": 8,
            "min": 1,
            "url": "https://api.provider.example/apps/web/scale"
          }
        ]
      },
      "security_headers": {
        "description": "Security response-header preset: `true` injects HSTS,\n`X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff` and\n`Referrer-Policy`; a mapping picks headers individually (services may\noverride with their own `security_headers`).",
        "anyOf": [
          {
            "$ref": "#/$defs/SecurityHeaders"
          },
          {
            "type": "null"
          }
        ],
        "examples": [
          true,
          {
            "frame_options": "SAMEORIGIN",
            "hsts": true
          }
        ]
      },
      "serve": {
        "description": "**Not accepted in a config file since 0.9.0; CLI and environment only.** Serve a local\ndirectory of static files instead of forwarding to a backend. Write it\nas a `services:` entry's `serve:`; the CLI's `--serve` and\n`APERIO_SERVE` are unaffected.",
        "type": [
          "string",
          "null"
        ],
        "deprecated": true,
        "examples": [
          "./dist"
        ]
      },
      "serve_404": {
        "description": "Static-file mode: HTML file served with status 404 for misses the SPA\nfallback does not cover (env: APERIO_SERVE_404). Process-wide, like\n`serve_spa`.",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "./dist/404.html"
        ]
      },
      "serve_spa": {
        "description": "Static-file mode: answer a navigation (`Accept: text/html`) that matches\nno file with the root `index.html` and status 200, so a client-side\nrouter owns its routes. A missing asset still 404s\n(env: APERIO_SERVE_SPA). Process-wide: it applies to every served\ndirectory of this client. Default: `false`.",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          true
        ]
      },
      "server": {
        "description": "The Aperio server to reach and the token to authenticate the tunnel with.",
        "anyOf": [
          {
            "$ref": "#/$defs/ServerValue"
          },
          {
            "type": "null"
          }
        ],
        "examples": [
          {
            "token": "apr_xxxxxxxxxxxxxxxx",
            "url": "https://tunnel.example.com"
          }
        ]
      },
      "services": {
        "description": "Expose several backends from one client, each on its own tunnel connection\n(replaces the single top-level `target`).",
        "type": [
          "array",
          "null"
        ],
        "examples": [
          [
            {
              "hostname": "app.example.com",
              "name": "web",
              "target": "http://localhost:3000"
            },
            {
              "hostname": "api.example.com",
              "name": "api",
              "target": "http://localhost:4000"
            }
          ]
        ],
        "items": {
          "$ref": "#/$defs/ServiceEntry"
        }
      },
      "startup_delay": {
        "description": "Seconds to wait before this service opens its tunnel. For a backend that\nis starting alongside the client and is not ready to answer the moment\nthe process is. Default: `0` (env: APERIO_STARTUP_DELAY).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "examples": [
          5
        ],
        "minimum": 0
      },
      "subscribe": {
        "description": "Topic filters this client subscribes to, for messages from the other\nclients of its organization. MQTT filter syntax: `+` is one level, `#`\nis the rest. `$aperio/...` carries the server's own events.\n\nAn entry is a bare filter, or an object that also names a command to run\nwhen a message arrives.",
        "type": [
          "array",
          "null"
        ],
        "examples": [
          [
            "deploy/web",
            {
              "run": "./deploy.sh",
              "timeout": 120,
              "topic": "deploy/api"
            }
          ]
        ],
        "items": {
          "$ref": "#/$defs/SubscribeValue"
        }
      },
      "target": {
        "description": "**Not accepted in a config file since 0.9.0; CLI and environment only.** Local backend to\nexpose. Write it as a `services:` entry instead; single-service mode\nstays available as the CLI's positional target and `APERIO_TARGET`.\n`h2c://` / `h2://` targets are dialed over HTTP/2 (gRPC).",
        "type": [
          "string",
          "null"
        ],
        "deprecated": true,
        "examples": [
          "http://localhost:3000",
          "3000",
          "h2c://127.0.0.1:50051"
        ]
      },
      "target_health": {
        "description": "**Not accepted in a config file since 0.9.0; CLI and environment only.** Backend health\nendpoint to probe; also the old flat spelling of `health.endpoint`.\nWrite it on the `services:` entry whose backend it probes.",
        "type": [
          "string",
          "null"
        ],
        "deprecated": true,
        "examples": [
          "/health"
        ]
      },
      "tcp_target": {
        "description": "**Not accepted in a config file since 0.9.0; CLI and environment only.** Raw TCP backend to\nexpose instead of HTTP. Write it as a `services:` entry's `tcp_target:`;\n`--tcp-target` and `APERIO_TCP_TARGET` are unaffected.",
        "type": [
          "string",
          "null"
        ],
        "deprecated": true,
        "examples": [
          "127.0.0.1:5432"
        ]
      },
      "timeout": {
        "description": "Seconds to wait for the backend to respond before failing a request.\nDefault: `30`.",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "examples": [
          30
        ],
        "minimum": 0
      },
      "token": {
        "description": "Deprecated spelling of `server.token`, kept so a token written at the\ntop level still authenticates rather than being silently ignored.",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "apr_xxxxxxxxxxxxxxxx"
        ]
      },
      "trim_bind": {
        "description": "Strip the path prefix before forwarding, so the backend sees `/` not the\nbind. Default: `true` when a path bind is set.",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          true
        ]
      },
      "tunnels": {
        "description": "Private local services a peer client may reach via `--bind-tunnels`; never\nexposed to the public web.",
        "type": [
          "array",
          "null"
        ],
        "examples": [
          [
            {
              "name": "pg_main",
              "target": "127.0.0.1:5432"
            },
            {
              "name": "dns",
              "protocol": "tcp/udp",
              "target": "127.0.0.1:53"
            }
          ]
        ],
        "items": {
          "$ref": "#/$defs/TunnelDecl"
        }
      },
      "version": {
        "description": "The Aperio version this file was written for, e.g. `0.5.0`. On startup\nthe client compares it against its own build and reports every recorded\nchange to the configuration format that landed in between, refusing to\nstart when one of them has security consequences. Unset disables the\ncheck (env: APERIO_VERSION).",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "0.5.0"
        ]
      },
      "wait_for_backend": {
        "description": "Hold the service out of routing until the backend first accepts a\nconnection, avoiding connection-refused errors while it boots\n(superseded by `target_health` when that is set). Deprecated spelling of\n`health.wait_for_backend`.",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          true
        ]
      },
      "webhook_inbox": {
        "description": "Persist inbound POST requests (third-party webhooks) into the server's\nwebhook inbox, for browsing and re-firing (services may override).\nDefault: `false`.",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          true
        ]
      }
    },
    "$defs": {
      "BindTunnelEntry": {
        "description": "What to bind, and where to bind it: one named tunnel (or, in the older\nspelling, every tunnel of one peer client).",
        "type": "object",
        "properties": {
          "address": {
            "description": "Local address the listener binds; anything but the default puts a\ndeliberately unexposed service on the network and is warned about.\nDefault: `127.0.0.1`.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "127.0.0.1"
            ]
          },
          "override": {
            "description": "Map a declared tunnel target to a specific local port instead of reusing\nthe target's. Only meaningful for an entry keyed by a peer's client id,\nwhich binds every tunnel that peer declares; a name-keyed entry is one\ntunnel and uses `port`.",
            "type": "object",
            "additionalProperties": {
              "type": "integer",
              "format": "uint16",
              "maximum": 65535,
              "minimum": 0
            },
            "default": {},
            "examples": [
              {
                "pg_main": 15432,
                "redis": 16379
              }
            ]
          },
          "port": {
            "description": "Local port for this tunnel. Unset reuses the declared target's port when\nit is free and unprivileged, and otherwise picks a stable port derived\nfrom the tunnel's name (logged at startup).",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint16",
            "examples": [
              15432
            ],
            "maximum": 65535,
            "minimum": 0
          },
          "psk": {
            "description": "Pre-shared key for this peer's end-to-end encrypted tunnels; must match\nthe `psk` the declaring client configured. Never sent to the server.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "a-long-shared-secret-both-sides-hold"
            ]
          },
          "token": {
            "description": "Token to authenticate the binding with; falls back to this client's\nserver token. Only needed when the tunnel is reached with a different\ncredential than the one this client connects with.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "apr_xxxxxxxxxxxxxxxx"
            ]
          }
        },
        "additionalProperties": false
      },
      "BindTunnelValue": {
        "description": "One `bind-tunnels:` entry, keyed by the tunnel's name (or, in the older\nspelling, by a peer client's id).\n\nA bare port number is the short form of `{ port: <n> }`, since naming the\nlocal port is the only thing most entries do.",
        "anyOf": [
          {
            "description": "`pg_main: 15432`, the local port, everything else defaulted.",
            "type": "integer",
            "format": "uint16",
            "maximum": 65535,
            "minimum": 0
          },
          {
            "description": "The full entry.",
            "$ref": "#/$defs/BindTunnelEntry"
          }
        ]
      },
      "CircuitBreakerConfig": {
        "description": "Refusing to dial a backend that keeps failing (`circuit_breaker:` on a\nservice, or at the top level as the default for every entry). Answers 502\nimmediately while open, so a dead backend stops being hammered and the\nvisitor stops waiting for a connection that will not come.",
        "type": "object",
        "properties": {
          "failures": {
            "description": "Consecutive failures that open the breaker. `0` disables it.\nDefault: `0` (off).",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint32",
            "examples": [
              5
            ],
            "minimum": 0
          },
          "open_for": {
            "description": "Seconds the breaker stays open before one request is let through to\ntest the backend again. Default: `30`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              30
            ],
            "minimum": 0
          }
        },
        "additionalProperties": false
      },
      "ConnectionRange": {
        "description": "The `{min, max}` spelling of `connections:`.",
        "type": "object",
        "properties": {
          "max": {
            "description": "Most connections the pool may grow to under load. Default: `min`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint32",
            "examples": [
              8
            ],
            "minimum": 0
          },
          "min": {
            "description": "Connections opened at startup and never dropped. Default: `1`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint32",
            "examples": [
              1
            ],
            "minimum": 0
          }
        }
      },
      "Connections": {
        "description": "`connections:` as written in the file: a fixed count, or an elastic pool\nwith a floor and a ceiling.\n\nThe scalar keeps meaning exactly what it always did, N connections opened\nat startup and kept, so no existing file changes behavior. Elasticity is\nsomething an operator opts into by writing a range, which is the honest\ndefault: our own measurements have a peak, past which more connections cost\nmore than they return, so growing a pool without being asked would not be\na safe assumption.",
        "anyOf": [
          {
            "description": "`connections: 4`, opened at startup and kept.",
            "type": "integer",
            "format": "uint32",
            "minimum": 0
          },
          {
            "description": "`connections: {min: 1, max: 8}`, grown and shrunk with load.",
            "$ref": "#/$defs/ConnectionRange"
          }
        ]
      },
      "HeaderDirectives": {
        "description": "Header edits applied to one direction of proxied traffic (request or\nresponse): `add` sets headers (replacing any existing value of the same\nname), `remove` strips headers by name (case-insensitive).",
        "type": "object",
        "properties": {
          "add": {
            "description": "Headers to set, name → value; replaces an existing header of the same name.",
            "type": "object",
            "additionalProperties": {
              "type": "string"
            },
            "default": {},
            "examples": [
              {
                "X-Forwarded-Env": "staging"
              }
            ]
          },
          "remove": {
            "description": "Header names to strip (case-insensitive).",
            "type": "array",
            "default": [],
            "examples": [
              [
                "Server",
                "X-Powered-By"
              ]
            ],
            "items": {
              "type": "string"
            }
          }
        }
      },
      "HeaderRules": {
        "description": "Header add/remove rules for proxied HTTP traffic: `request` edits what the\nlocal backend receives, `response` edits what the visitor receives.\nHop-by-hop and tunnel-critical headers stay managed by Aperio regardless.",
        "type": "object",
        "properties": {
          "request": {
            "description": "Edits applied to forwarded requests before they reach the local backend.",
            "anyOf": [
              {
                "$ref": "#/$defs/HeaderDirectives"
              },
              {
                "type": "null"
              }
            ],
            "examples": [
              {
                "add": {
                  "X-Forwarded-Env": "staging"
                },
                "remove": [
                  "X-Internal-Debug"
                ]
              }
            ]
          },
          "response": {
            "description": "Edits applied to backend responses before they return to the visitor.",
            "anyOf": [
              {
                "$ref": "#/$defs/HeaderDirectives"
              },
              {
                "type": "null"
              }
            ],
            "examples": [
              {
                "add": {
                  "X-Served-By": "aperio"
                },
                "remove": [
                  "X-Powered-By"
                ]
              }
            ]
          }
        }
      },
      "HealthConfig": {
        "description": "Backend health probing for one service: the endpoint the client checks and\nhow patient it is before pulling the backend out of routing.\n\nThe flat `target_health` / `health_interval` / `health_timeout` /\n`health_threshold` / `wait_for_backend` keys mean exactly the same thing\nand still work; this block is the form to write new configs in, and wins\nper field when both are present.",
        "type": "object",
        "properties": {
          "endpoint": {
            "description": "Endpoint to probe: a path like `/health` or a full URL. Unset = no probe,\nand the service is routable as soon as the tunnel is up.\n\nAgainst an `h2c://`/`h2://` target this names the **gRPC service** to\nhealth-check instead: the probe calls `grpc.health.v1.Health/Check`,\nbecause a plain GET cannot reach a server that speaks HTTP/2 with prior\nknowledge and routes by method name. `/` asks about the server as a\nwhole, which is the usual answer. An absolute `http(s)://` URL still\nmeans an ordinary HTTP probe, for a backend exposing health on a\nseparate port.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "/health"
            ]
          },
          "interval": {
            "description": "Seconds between probes. Default: `10`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              10
            ],
            "minimum": 0
          },
          "threshold": {
            "description": "Failed probes in a row before the backend is reported unhealthy.\nDefault: `2`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint32",
            "examples": [
              3
            ],
            "minimum": 0
          },
          "timeout": {
            "description": "Seconds to wait for each probe before counting it as failed.\nDefault: `5`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              5
            ],
            "minimum": 0
          },
          "wait_for_backend": {
            "description": "Hold the service out of routing until the backend first accepts a\nconnection, avoiding connection-refused errors while it boots\n(superseded by `endpoint` when that is set). Default: `false`.",
            "type": [
              "boolean",
              "null"
            ],
            "examples": [
              true
            ]
          }
        },
        "additionalProperties": false
      },
      "Hostnames": {
        "description": "A service's public hostname(s): either a single `hostname: app.example.com`\nor a list `hostname: [app.example.com, www.example.com]`. Each must be\npermitted by the client's token.",
        "anyOf": [
          {
            "description": "A single hostname.",
            "type": "string"
          },
          {
            "description": "Several hostnames routing to the same service.",
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        ]
      },
      "OtelBridge": {
        "description": "The client's `otel_bridge:` block.\n\n`PartialEq` so a config reload can notice that this block changed: the\nbridge is the one facility a running client cannot rebuild, so the change\nis reported rather than silently ignored.",
        "type": "object",
        "properties": {
          "listen": {
            "description": "Address for the OTLP/HTTP receiver, the one every SDK can reach with\n`OTEL_EXPORTER_OTLP_ENDPOINT`. Default: `127.0.0.1:4318`.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "127.0.0.1:4318"
            ]
          },
          "listen_grpc": {
            "description": "Address for the OTLP/gRPC receiver, for SDKs pinned to that transport.\nUnset = no gRPC listener. Conventionally port 4317.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "127.0.0.1:4317"
            ]
          },
          "queue": {
            "description": "Exports to hold when the far end is not keeping up. Past this, the\nnewest is dropped and counted, never waited on: an exporter that cannot\nhand off its batch blocks the application it is instrumenting, so\ntelemetry must never be the reason a tunnel stalls. Default: `256`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint",
            "examples": [
              256
            ],
            "minimum": 0
          },
          "transport": {
            "description": "How exports reach the server: `tunnel` sends them as frames on the\nWebSocket this client already holds, `https` posts them to the server's\nendpoint as ordinary requests. Default: `tunnel`.\n\n`tunnel` is the one that keeps the \"exactly one outbound connection\"\nproperty that makes this worth having; `https` exists because a client\nwhose telemetry is bursty may prefer it to stay off the tunnel entirely,\nwhere it would share flow control with proxied traffic.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "tunnel",
              "https"
            ]
          }
        },
        "additionalProperties": false
      },
      "RetryConfig": {
        "description": "Retrying a backend request that failed before any response arrived\n(`retry:` on a service, or at the top level as the default for every\nentry).",
        "type": "object",
        "properties": {
          "all_methods": {
            "description": "Retry non-idempotent methods (POST, PATCH) as well. Off by default,\nbecause a retried write may reach the backend twice; the same reasoning\nas the server's `failover.all_methods`. Default: `false`.",
            "type": [
              "boolean",
              "null"
            ],
            "examples": [
              true
            ]
          },
          "attempts": {
            "description": "Total attempts, including the first. `1` disables retrying.\nDefault: `1`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint32",
            "examples": [
              3
            ],
            "minimum": 0
          },
          "backoff": {
            "description": "Milliseconds to wait before the second attempt, doubled before each\nfurther one. Default: `100`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              100
            ],
            "minimum": 0
          }
        },
        "additionalProperties": false
      },
      "ScalingDecl": {
        "description": "Autoscaling declaration: the URL the *server* calls when this service needs\ncapacity it does not have. Aperio never starts or stops anything itself, it\nonly signals a desired capacity to an endpoint the operator controls.\n\nThe record outlives the client process on purpose: the whole point of\n`min: 0` is that the server can call this URL when nothing is running.",
        "type": "object",
        "properties": {
          "cold_start": {
            "description": "How long a visitor request may be held while a cold start completes,\ne.g. `45s` (0 = do not hold, answer immediately). Default: `45s`.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "45s"
            ]
          },
          "cooldown": {
            "description": "Minimum gap between two calls for this bind, e.g. `60s`.\nA new instance needs time to appear; without this the server would ask\nagain while it is still starting. Default: `60s`.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "60s"
            ]
          },
          "max": {
            "description": "Ceiling the server will never ask to exceed (0 = only cold starts, no\nscale-out). Default: `0`.",
            "type": "integer",
            "format": "uint32",
            "default": 0,
            "minimum": 0
          },
          "min": {
            "description": "Instances that should always be running. `0` opts into scale-to-zero:\na request for an unserved hostname triggers a cold start instead of a\n504. Default: `0`.",
            "type": "integer",
            "format": "uint32",
            "default": 0,
            "minimum": 0
          },
          "secret": {
            "description": "Sent as `Authorization: Bearer` on the outgoing call. Write-only: the\nserver never echoes it back and never logs it.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "${SCALE_SECRET}"
            ]
          },
          "target_utilization": {
            "description": "Pool utilization above which the server asks for one more instance,\nbetween 0 and 1. Default: `0.8`.",
            "type": [
              "number",
              "null"
            ],
            "format": "double",
            "examples": [
              0.8
            ]
          },
          "url": {
            "description": "Endpoint the server POSTs to when it wants more capacity. HTTPS only\nunless the server allows plain HTTP; private and loopback addresses are\nrefused, since the caller is a lower-trust credential than an operator.",
            "type": "string",
            "examples": [
              "https://api.provider.example/apps/web/scale"
            ]
          },
          "window": {
            "description": "How long utilization must stay above the target before scaling out,\ne.g. `15s`. Guards against reacting to a single spike. Default: `15s`.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "15s"
            ]
          }
        },
        "required": [
          "url"
        ]
      },
      "SecurityHeaderOptions": {
        "description": "Individually selected security response headers; only the set fields are\ninjected.",
        "type": "object",
        "properties": {
          "csp": {
            "description": "`Content-Security-Policy` value to inject (no default, CSP is\napplication-specific).",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "default-src 'self'"
            ]
          },
          "frame_options": {
            "description": "`X-Frame-Options` value to inject.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "DENY",
              "SAMEORIGIN"
            ]
          },
          "hsts": {
            "description": "Inject `Strict-Transport-Security` (only meaningful behind HTTPS).",
            "type": [
              "boolean",
              "null"
            ],
            "examples": [
              true
            ]
          },
          "hsts_max_age": {
            "description": "HSTS `max-age` in seconds. Default: `63072000` (2 years).",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              31536000
            ],
            "minimum": 0
          },
          "nosniff": {
            "description": "Inject `X-Content-Type-Options: nosniff`.",
            "type": [
              "boolean",
              "null"
            ],
            "examples": [
              true
            ]
          },
          "referrer_policy": {
            "description": "`Referrer-Policy` value to inject.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "strict-origin-when-cross-origin"
            ]
          }
        },
        "additionalProperties": false
      },
      "SecurityHeaders": {
        "description": "Security response-header preset: `security_headers: true` enables the\nstandard set (HSTS, `X-Frame-Options: DENY`, `X-Content-Type-Options:\nnosniff`, `Referrer-Policy: strict-origin-when-cross-origin`), a mapping\npicks headers individually. Explicit `headers:` rules always win over the\npreset.",
        "anyOf": [
          {
            "description": "`true` enables the standard preset, `false` disables it (e.g. for one\nservice when the top level enables it).",
            "type": "boolean"
          },
          {
            "description": "Granular per-header selection.",
            "$ref": "#/$defs/SecurityHeaderOptions"
          }
        ]
      },
      "ServerValue": {
        "description": "The Aperio server this client connects to: either a bare URL string, or a\n`{ url, token }` section that also carries the tunnel token.",
        "anyOf": [
          {
            "description": "Server URL only, the token then comes from `token:` or the environment.",
            "type": "string"
          },
          {
            "description": "Server URL together with the tunnel token.",
            "type": "object",
            "properties": {
              "api_key": {
                "description": "Admin API key used by `aperio-client api ...` calls (never for the\ntunnel itself).",
                "type": [
                  "string",
                  "null"
                ],
                "examples": [
                  "apk_xxxxxxxxxxxxxxxx"
                ]
              },
              "token": {
                "description": "Tunnel token (master or a scoped dynamic token) that authorizes this client.",
                "type": [
                  "string",
                  "null"
                ],
                "examples": [
                  "apr_xxxxxxxxxxxxxxxx"
                ]
              },
              "url": {
                "description": "URL of the Aperio server this client dials out to.",
                "type": [
                  "string",
                  "null"
                ],
                "examples": [
                  "https://tunnel.example.com"
                ]
              },
              "urls": {
                "description": "Additional server URLs to fail over to, tried in order when `url` is\nunreachable, for a redundant control plane.",
                "type": [
                  "array",
                  "null"
                ],
                "examples": [
                  [
                    "https://tunnel-b.example.com"
                  ]
                ],
                "items": {
                  "type": "string"
                }
              }
            }
          }
        ]
      },
      "ServiceEntry": {
        "description": "One exposed backend when a single client serves several at once; any unset\nfield falls back to the top-level value.",
        "type": "object",
        "properties": {
          "adaptive_concurrency": {
            "description": "Lower the announced `max_concurrent` while requests queue up waiting for\na local permit, and climb back when they stop. The server already queues\nrather than dispatching past the announced number, so this is how a\nclient that has become slow stops being sent work it cannot do; the\nserver then holds the request, picks another client, or asks for capacity\nthrough autoscaling, all of which beat a refusal. Needs `max_concurrent`\nto be set, since that is the number being moved. Default: `false`\n(env: APERIO_ADAPTIVE_CONCURRENCY).",
            "type": [
              "boolean",
              "null"
            ],
            "examples": [
              true
            ]
          },
          "allowed_ips": {
            "description": "Visitor IPs/CIDRs allowed to reach this service (plain IPs or CIDR\nranges); empty/unset = everyone. Enforced by the server before dispatch.",
            "type": [
              "array",
              "null"
            ],
            "examples": [
              [
                "203.0.113.7",
                "10.0.0.0/8"
              ]
            ],
            "items": {
              "type": "string"
            }
          },
          "auth": {
            "description": "Gate this service behind your own `user:password` login instead of the server's.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "admin:s3cret"
            ]
          },
          "bandwidth": {
            "description": "This service's share of the link: the server never pushes it faster than\nthis, and the share is split across its `connections`. Settled against the\ntop-level `bandwidth` budget when there is one. Bit suffixes\n(`kbit`/`mbit`/`gbit`) count as /8, byte suffixes (`kb`/`mb`/`gb`, or bare\n`k`/`m`/`g`) as x1000.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "8mbit",
              "500kbit",
              "2MB"
            ]
          },
          "cache": {
            "description": "Let the server cache this service's GET responses (per their\n`Cache-Control`); effective only when the server enables APERIO_CACHE.",
            "type": [
              "boolean",
              "null"
            ],
            "examples": [
              true
            ]
          },
          "capture": {
            "description": "Record this service's transactions for the dashboard's request\ninspector. Turning it off for a service that carries\nheavy traffic buys back a mutex, two header clones and a capture entry\nper request, at the cost of not being able to inspect or replay its\nrequests afterwards. Default: `true`.",
            "type": [
              "boolean",
              "null"
            ],
            "examples": [
              false
            ]
          },
          "circuit_breaker": {
            "description": "Circuit breaker for this service's backend; falls back to the top-level\n`circuit_breaker:`.",
            "anyOf": [
              {
                "$ref": "#/$defs/CircuitBreakerConfig"
              },
              {
                "type": "null"
              }
            ],
            "examples": [
              {
                "failures": 5,
                "open_for": 30
              }
            ]
          },
          "connect_timeout": {
            "description": "Seconds to wait for the TCP connection to this backend before giving\nup, separate from `timeout`, which covers the whole request. A backend\nacross a VPN needs longer than one on loopback, and one number for both\nmeans either slow failure detection everywhere or spurious failures for\nthe far one. Default: unset (the whole-request `timeout` applies)\n(env: APERIO_CONNECT_TIMEOUT).",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              2
            ],
            "minimum": 0
          },
          "connections": {
            "description": "Parallel tunnel connections opened for this service; the\nserver's `max_connections_per_service` is the ceiling, announced on\nconnect, and a token may lower it further;\nthe server load-balances across them like separate clients, so a single\ndropped connection leaves no visitor-facing gap.\nA number opens exactly that many at startup; `{min: 1, max: 8}` opens the\nfloor and grows towards the ceiling while requests queue up, then shrinks\nback when they stop. Default: `1`.",
            "anyOf": [
              {
                "$ref": "#/$defs/Connections"
              },
              {
                "type": "null"
              }
            ],
            "examples": [
              2,
              {
                "max": 8,
                "min": 1
              }
            ]
          },
          "custom_name": {
            "description": "What to call this service on screen. Free text: any language, any\npunctuation, spaces. Nothing addresses it, so nothing breaks when it\nchanges.",
            "type": [
              "string",
              "null"
            ],
            "default": null,
            "examples": [
              "Public Web"
            ]
          },
          "denied": {
            "description": "Redirect URL for visitors rejected by `allowed_ips` when no candidate\nof the route admits them (unset = stealth: the same answer as an\nunclaimed route).",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "https://example.com/not-for-you"
            ]
          },
          "depends_on": {
            "description": "Names of services in this same file that must have a live tunnel before\nthis one opens its own. Waits for them, then proceeds regardless after a\nbounded grace period, because a dependency that never arrives must not\nkeep a service that could serve traffic off the air forever.",
            "type": [
              "array",
              "null"
            ],
            "examples": [
              [
                "api"
              ]
            ],
            "items": {
              "type": "string"
            }
          },
          "headers": {
            "description": "Request/response header add-remove rules for this service (replaces the\ntop-level `headers` when set).",
            "anyOf": [
              {
                "$ref": "#/$defs/HeaderRules"
              },
              {
                "type": "null"
              }
            ],
            "examples": [
              {
                "request": {
                  "add": {
                    "X-Forwarded-Env": "staging"
                  },
                  "remove": [
                    "X-Internal-Debug"
                  ]
                },
                "response": {
                  "add": {
                    "X-Served-By": "aperio"
                  },
                  "remove": [
                    "X-Powered-By"
                  ]
                }
              }
            ]
          },
          "health": {
            "description": "Backend health probing for this service (`endpoint`, `interval`,\n`timeout`, `threshold`, `wait_for_backend`). Preferred over the flat\n`target_health` / `health_*` keys, which still work.",
            "anyOf": [
              {
                "$ref": "#/$defs/HealthConfig"
              },
              {
                "type": "null"
              }
            ],
            "examples": [
              {
                "endpoint": "/health",
                "interval": 10,
                "threshold": 2
              }
            ]
          },
          "health_interval": {
            "description": "Seconds between backend health probes. Deprecated spelling of\n`health.interval`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              10
            ],
            "minimum": 0
          },
          "health_threshold": {
            "description": "Failed probes in a row before the backend is reported unhealthy.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint32",
            "examples": [
              3
            ],
            "minimum": 0
          },
          "health_timeout": {
            "description": "Seconds to wait for each health probe before counting it as failed.\nDeprecated spelling of `health.timeout`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              5
            ],
            "minimum": 0
          },
          "hostname": {
            "description": "Public hostname(s) that should route to this service: a single string\nor a list. Each must be permitted by the client's token.",
            "anyOf": [
              {
                "$ref": "#/$defs/Hostnames"
              },
              {
                "type": "null"
              }
            ],
            "examples": [
              "app.example.com",
              [
                "app.example.com",
                "www.example.com"
              ]
            ]
          },
          "max_concurrent": {
            "description": "Most requests this service handles at once before the server queues the rest.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint32",
            "examples": [
              8
            ],
            "minimum": 0
          },
          "max_redirects": {
            "description": "How many backend redirects to follow transparently before passing one through.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint",
            "examples": [
              5
            ],
            "minimum": 0
          },
          "max_request_body": {
            "description": "Largest request body, in bytes, visitors may upload to this service;\nthe server rejects bigger uploads with 413 before they enter the tunnel.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              1048576
            ],
            "minimum": 0
          },
          "max_response_body": {
            "description": "Largest response body, in bytes, this service will relay to a visitor.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint",
            "examples": [
              10485760
            ],
            "minimum": 0
          },
          "metrics_labels": {
            "description": "Static Prometheus labels attached to this client's own metric series,\ne.g. `{env: prod, region: eu-west}`, so one Prometheus can serve several\nenvironments without relabelling rules. At most 8 labels; names must be\n`[a-zA-Z_][a-zA-Z0-9_]*` and the server drops anything else, including\nthe names it writes itself (`client_id`, `job`, `instance`, …).",
            "type": [
              "object",
              "null"
            ],
            "additionalProperties": {
              "type": "string"
            },
            "examples": [
              {
                "env": "prod",
                "region": "eu-west"
              }
            ]
          },
          "min_tls_version": {
            "description": "Lowest TLS version accepted from an `https://` backend: `1.2` or `1.3`.\nPer service because a fleet with one legacy backend should not have to\nlower the floor for all of them. Default: rustls's own floor\n(env: APERIO_MIN_TLS_VERSION).",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "1.3"
            ]
          },
          "name": {
            "description": "Handle for this service in client logs and the dashboard clients table.\nAn identifier: a-z, 0-9 and `_`. Use `custom_name` for something to read.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "web"
            ]
          },
          "pass_hostname": {
            "description": "Forward the visitor's original Host header instead of the target's.",
            "type": [
              "boolean",
              "null"
            ],
            "examples": [
              false
            ]
          },
          "path": {
            "description": "Public path prefix that should route to this service.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "/api"
            ]
          },
          "priority": {
            "description": "Failover tier for this service (0 = primary, higher numbers are standbys).",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint32",
            "examples": [
              0
            ],
            "minimum": 0
          },
          "public": {
            "description": "Serve this service without the server's visitor login (needs a token that allows it).",
            "type": [
              "boolean",
              "null"
            ],
            "examples": [
              true
            ]
          },
          "resilience": {
            "description": "Keep serving this service's cached responses (marked, even past their\nlifetime) while no healthy client is connected, instead of failing with\n504 (needs `cache: true` and the server-side cache enabled).",
            "type": [
              "boolean",
              "null"
            ],
            "examples": [
              true
            ]
          },
          "response_timeout": {
            "description": "Seconds the server should wait for this service to answer a dispatched\nrequest before failing it, a per-service override of the server's global\ngateway response timeout, for slow report/upload endpoints.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              120
            ],
            "minimum": 0
          },
          "retry": {
            "description": "Retry policy for this service's backend requests; falls back to the\ntop-level `retry:`.",
            "anyOf": [
              {
                "$ref": "#/$defs/RetryConfig"
              },
              {
                "type": "null"
              }
            ],
            "examples": [
              {
                "attempts": 3,
                "backoff": 100
              }
            ]
          },
          "security_headers": {
            "description": "Security response-header preset for this service (`true` or a granular\nmapping; replaces the top-level `security_headers` when set).",
            "anyOf": [
              {
                "$ref": "#/$defs/SecurityHeaders"
              },
              {
                "type": "null"
              }
            ],
            "examples": [
              true,
              {
                "frame_options": "SAMEORIGIN",
                "hsts": true
              }
            ]
          },
          "serve": {
            "description": "Serve a local directory of static files as this service instead of\nforwarding to a backend (mutually exclusive with `target`/`tcp_target`);\ndirectories serve their `index.html`.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "./dist"
            ]
          },
          "startup_delay": {
            "description": "Seconds to wait before this service opens its tunnel. For a backend that\nis starting alongside the client and is not ready to answer the moment\nthe process is. Default: `0` (env: APERIO_STARTUP_DELAY).",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              5
            ],
            "minimum": 0
          },
          "target": {
            "description": "Local backend this service exposes through the tunnel; `h2c://` /\n`h2://` targets are dialed over HTTP/2 (gRPC backends, trailers relayed);\n`unix://` targets forward over a Unix domain socket.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "http://localhost:3000",
              "3000",
              "h2c://127.0.0.1:50051",
              "unix:///var/run/app.sock"
            ]
          },
          "target_health": {
            "description": "Backend health endpoint the client probes to pull itself from rotation\nwhen down. Deprecated spelling of `health.endpoint`.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "/health"
            ]
          },
          "tcp_target": {
            "description": "Raw TCP backend for this service instead of HTTP (experimental).",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "127.0.0.1:5432"
            ]
          },
          "timeout": {
            "description": "Seconds to wait for this backend to respond before failing the request.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              30
            ],
            "minimum": 0
          },
          "trim_bind": {
            "description": "Strip the path prefix before forwarding, so the backend sees `/` not the bind.",
            "type": [
              "boolean",
              "null"
            ],
            "examples": [
              true
            ]
          },
          "wait_for_backend": {
            "description": "Hold this service out of routing until the backend first accepts a\nconnection, avoiding connection-refused errors while it boots\n(superseded by `target_health` when that is set).",
            "type": [
              "boolean",
              "null"
            ],
            "examples": [
              true
            ]
          },
          "webhook_inbox": {
            "description": "Persist inbound POST requests (third-party webhooks) hitting this\nservice into the server's webhook inbox, for browsing and re-firing.",
            "type": [
              "boolean",
              "null"
            ],
            "examples": [
              true
            ]
          }
        }
      },
      "SubscribeEntry": {
        "description": "A subscription that may also run a command when a message arrives.\n\n`run:` is a remote-execution primitive by design: a message from another\nclient of the organization causes a command to run here. Everything about\nits shape follows from that. The payload never reaches the command line,\nonly stdin and the environment, so a message can never become part of the\ncommand. Concurrency is capped and the run is timed, so a publisher in a\nloop cannot fork a thousand processes or leave one wedged forever. And it\nis per topic, opt-in, in a file the operator wrote.",
        "type": "object",
        "properties": {
          "env": {
            "description": "Extra environment variables for `run:`, on top of what the client sets.\nThe command inherits the client's own environment as well; this is for\nthe values that belong to *this* subscription. `APERIO_MESSAGE_TOPIC`\nand `APERIO_MESSAGE_ID` are set after these and cannot be overridden,\nsince a command reading them must be able to trust what it finds.",
            "type": "object",
            "additionalProperties": {
              "type": "string"
            },
            "default": {},
            "examples": [
              {
                "DEPLOY_ENV": "staging",
                "SLACK_CHANNEL": "#ops"
              }
            ]
          },
          "max_concurrent": {
            "description": "Runs allowed at once for this subscription. Messages that\narrive while the cap is reached are dropped with a warning rather than\nqueued: a queue for a command that cannot keep up is the same problem\none step later. Default: `1`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint32",
            "examples": [
              1
            ],
            "minimum": 0
          },
          "run": {
            "description": "Command to run for each message, through the shell. The message body\narrives on **stdin**, never as an argument; `APERIO_MESSAGE_TOPIC` and\n`APERIO_MESSAGE_ID` are set in the environment. Unset = deliver only.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "./deploy.sh",
              "systemctl reload nginx"
            ]
          },
          "timeout": {
            "description": "Seconds a run may take before it is killed. A command that hangs must\nnot hold the subscription's one slot forever. Default: `60`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              60
            ],
            "minimum": 0
          },
          "topic": {
            "description": "The topic filter to subscribe to.",
            "type": "string",
            "examples": [
              "deploy/web",
              "$aperio/client/#"
            ]
          }
        },
        "additionalProperties": false,
        "required": [
          "topic"
        ]
      },
      "SubscribeValue": {
        "description": "One `subscribe:` entry.\n\nA bare filter is the short form: listen, and let whatever is attached to\nthe local face receive it. The object form is for a client that should\n*act* on the message itself.",
        "anyOf": [
          {
            "description": "`- deploy/web`, listen and deliver, nothing else.",
            "type": "string"
          },
          {
            "description": "The full entry, for a subscription that runs something.",
            "$ref": "#/$defs/SubscribeEntry"
          }
        ]
      },
      "TopHealthConfig": {
        "description": "The top level's `health:` block.\n\nThe same fields as [`HealthConfig`], a file written either way parses\nidentically, except that `endpoint` is on its way out here. The other\nchildren are real defaults: a `services:` entry that says nothing about\nits interval, timeout, threshold or boot wait inherits them. `endpoint` is\nnot, and never was: a probe path belongs to the backend it probes, so the\nresolver reads it strictly per entry and a top-level one is read by\nnothing at all once a `services:` list exists.",
        "type": "object",
        "properties": {
          "endpoint": {
            "description": "**Not accepted in a config file since 0.9.0; CLI and environment only.** Endpoint to probe.\nWrite it on the `services:` entry whose backend it probes; at the top\nlevel it is read only by a file that has no `services:` list.",
            "type": [
              "string",
              "null"
            ],
            "deprecated": true,
            "examples": [
              "/health"
            ]
          },
          "interval": {
            "description": "Seconds between probes. Applies to every `services:` entry that does not\nset its own. Default: `10`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              10
            ],
            "minimum": 0
          },
          "threshold": {
            "description": "Failed probes in a row before the backend is reported unhealthy. Applies\nto every `services:` entry that does not set its own. Default: `2`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint32",
            "examples": [
              3
            ],
            "minimum": 0
          },
          "timeout": {
            "description": "Seconds to wait for each probe before counting it as failed. Applies to\nevery `services:` entry that does not set its own. Default: `5`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              5
            ],
            "minimum": 0
          },
          "wait_for_backend": {
            "description": "Hold the service out of routing until the backend first accepts a\nconnection, avoiding connection-refused errors while it boots. Applies\nto every `services:` entry that does not set its own. Default: `false`.",
            "type": [
              "boolean",
              "null"
            ],
            "examples": [
              true
            ]
          }
        },
        "additionalProperties": false
      },
      "TunnelDecl": {
        "description": "A private local service (e.g. a database or SSH) this client makes reachable\nto a peer running `--bind-tunnels`, without ever exposing it to the public web.",
        "type": "object",
        "properties": {
          "custom_name": {
            "description": "What to call it on screen. Free text: any language, any punctuation,\nspaces. Nothing addresses it, so nothing breaks when it changes.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "Primary Postgres"
            ]
          },
          "encrypt": {
            "description": "End-to-end encrypt this tunnel between the two clients (X25519 +\nChaCha20-Poly1305); the server only relays ciphertext. TCP only.\nDefault: `false`.",
            "type": "boolean",
            "default": false
          },
          "expose": {
            "description": "Deprecated spelling of the public-port claim (TCP only): the value must\nequal the `key` of an `expose:` entry in the server's aperio-server.yaml.\nPrefer naming the tunnel and letting the server's `expose:` entry point\nat it by `tunnel:` + `token:`, which is revocable and names an owner.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "k5fj2q-expose-secret"
            ]
          },
          "idle_timeout": {
            "description": "UDP only: seconds a relay may sit with no datagrams in either direction\nbefore it expires; binders learn it via tunnel discovery. Default: `60`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              300
            ],
            "minimum": 0
          },
          "name": {
            "description": "Handle this tunnel is bound and exposed by, unique within the\norganization. Binders name it instead of naming a client id, so the\nhandle survives reconnects, parallel connections and a `services:` list.\nUnset derives one from the target, and a name shaped like a UUID is\nrejected so names can never collide with client ids.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "pg_main",
              "ssh_bastion"
            ]
          },
          "protocol": {
            "description": "Transport of the tunnel: `tcp`, `udp` (best-effort datagram\nrelay), or `tcp/udp` for a service that is genuinely both (DNS, for\ninstance). A combined tunnel is one tunnel with one name and one local\nport on the binder, answering on both transports. Default: `tcp`.",
            "type": "string",
            "default": "tcp",
            "examples": [
              "tcp",
              "udp",
              "tcp/udp"
            ]
          },
          "proxy_protocol": {
            "description": "Write a PROXY protocol v2 header to this backend before any payload\nbyte, announcing the visitor's real address. TCP only. Without it the\nbackend sees a connection from the client process and the visitor's\naddress is lost at the last hop. Turn it on only when the backend is\nconfigured to expect the header (nginx `listen ... proxy_protocol`,\nHAProxy `accept-proxy`, MySQL `proxy-protocol-networks`): a backend that\nis not will read it as protocol garbage and drop the connection.\nDefault: `false`.",
            "type": "boolean",
            "default": false,
            "examples": [
              true
            ]
          },
          "psk": {
            "description": "Pre-shared key mixed into the key derivation of an encrypted tunnel,\nprotecting against an actively hostile server. Never sent anywhere,\nthe binder configures the same value in its `bind-tunnels` entry.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "a-long-shared-secret-both-sides-hold"
            ],
            "writeOnly": true
          },
          "target": {
            "description": "Local address this client dials when a peer binds the tunnel.",
            "type": "string",
            "examples": [
              "127.0.0.1:27017"
            ]
          }
        },
        "required": [
          "target"
        ]
      }
    }
  },
  "server": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "ServerFileConfig",
    "description": "The `aperio-server.yaml` configuration file. The server is environment-\ndriven; every scalar key here is materialized into its `APERIO_*`\nenvironment variable at startup (the file takes precedence over the\nenvironment). Structured sections (`headers`, `routes`, `expose`) are read\ndirectly. Unknown keys are allowed and passed through as env vars.",
    "type": "object",
    "properties": {
      "503_page": {
        "description": "Custom 503 maintenance page path (env: APERIO_503_PAGE).",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "./pages/503.html"
        ]
      },
      "504_page": {
        "description": "Custom 504 error page path (env: APERIO_504_PAGE).",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "./pages/504.html"
        ]
      },
      "access_events": {
        "description": "Emit the per-request structured access event for a successful request.\nDistinct from `log_level`: `false` silences that\none-per-request line and leaves warnings and errors alone, so a refused\nor failed request still logs at `warn` (env: APERIO_ACCESS_EVENTS).\nDefault: `true`.",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          false
        ]
      },
      "access_log": {
        "description": "Structured access log path (env: APERIO_ACCESS_LOG).",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "/app/data/access.jsonl"
        ]
      },
      "access_log_sample_rate": {
        "description": "Fraction of *successful* requests that produce an access line, 0.0 to\n1.0. Default `1.0` = every request, which is what this always did.\nFailures are never sampled out: a sampled-away error is the one line\nsomebody needed. Applies to the `aperio_access` event and the\n`access_log` file alike (env: APERIO_ACCESS_LOG_SAMPLE_RATE).",
        "type": [
          "number",
          "null"
        ],
        "format": "double",
        "examples": [
          0.1
        ]
      },
      "admin_allowed_ips": {
        "description": "Source IPs/CIDRs allowed to reach the authenticated admin surface (the\ndashboard and `/aperio/api/*`); empty = no restriction. An invalid entry\nrefuses startup rather than applying a partial allowlist\n(env: APERIO_ADMIN_ALLOWED_IPS).",
        "type": [
          "array",
          "null"
        ],
        "examples": [
          [
            "10.0.0.0/8"
          ]
        ],
        "items": {
          "type": "string"
        }
      },
      "alert": {
        "description": "Alerting thresholds: when the server logs and emits an alert",
        "anyOf": [
          {
            "$ref": "#/$defs/AlertGroup"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "examples": [
          {
            "client_down": 60,
            "error_rate": 0.25,
            "min_requests": 20,
            "window": 300
          }
        ]
      },
      "alert_client_down": {
        "description": "Deprecated spelling of `alert.client_down` (env: APERIO_ALERT_CLIENT_DOWN).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "minimum": 0
      },
      "alert_error_rate": {
        "description": "Deprecated spelling of `alert.error_rate` (env: APERIO_ALERT_ERROR_RATE).",
        "type": [
          "number",
          "null"
        ],
        "format": "double"
      },
      "alert_min_requests": {
        "description": "Deprecated spelling of `alert.min_requests` (env: APERIO_ALERT_MIN_REQUESTS).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "minimum": 0
      },
      "alert_rules": {
        "description": "Operator-defined alert rules over what the server measures",
        "type": [
          "array",
          "null"
        ],
        "examples": [
          [
            {
              "above": 536870912,
              "for": 300,
              "metric": "store_bytes",
              "name": "disk-filling"
            }
          ]
        ],
        "items": {
          "$ref": "#/$defs/AlertRule"
        }
      },
      "alert_window": {
        "description": "Deprecated spelling of `alert.window` (env: APERIO_ALERT_WINDOW).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "minimum": 0
      },
      "alternate_servers": {
        "description": "Other Aperio servers a client of this one may fall back to, announced in\nthe handshake. A planned migration or a regional failover otherwise means\nediting every client's config; announce the new server here and clients\nlearn it on their next connection.\n\nAdvice, not instruction: a client appends these *after* the servers its\nown config names, so the operator's list still decides the order. A\nclient that has never reached this server learns nothing, which is why\nthis is for a migration announced in advance rather than a rescue\n(env: APERIO_ALTERNATE_SERVERS, comma-separated).",
        "type": [
          "array",
          "null"
        ],
        "examples": [
          [
            "wss://eu.tunnel.example.com/tunnel"
          ]
        ],
        "items": {
          "type": "string"
        }
      },
      "audit": {
        "description": "Audit-log file rotation",
        "anyOf": [
          {
            "$ref": "#/$defs/AuditGroup"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "examples": [
          {
            "max_files": 3,
            "max_size": 10485760
          }
        ]
      },
      "audit_max_files": {
        "description": "Deprecated spelling of `audit.max_files` (env: APERIO_AUDIT_MAX_FILES).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "minimum": 0
      },
      "audit_max_size": {
        "description": "Deprecated spelling of `audit.max_size` (env: APERIO_AUDIT_MAX_SIZE).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "minimum": 0
      },
      "backup": {
        "description": "Scheduled snapshots of the SQLite store",
        "anyOf": [
          {
            "$ref": "#/$defs/BackupGroup"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "examples": [
          {
            "dir": "/app/data/backups",
            "interval": 86400,
            "keep": 7
          }
        ]
      },
      "backup_dir": {
        "description": "Flat spelling of `backup.dir` (env: APERIO_BACKUP_DIR).",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "/app/data/backups"
        ]
      },
      "backup_interval": {
        "description": "Flat spelling of `backup.interval` (env: APERIO_BACKUP_INTERVAL).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "examples": [
          86400
        ],
        "minimum": 0
      },
      "backup_keep": {
        "description": "Flat spelling of `backup.keep` (env: APERIO_BACKUP_KEEP).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "examples": [
          7
        ],
        "minimum": 0
      },
      "cache": {
        "description": "The server-side GET response cache",
        "anyOf": [
          {
            "$ref": "#/$defs/CacheSetting"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "examples": [
          true,
          {
            "enabled": true,
            "max_bytes": 67108864,
            "max_stale": 3600
          }
        ]
      },
      "cache_max_bytes": {
        "description": "Deprecated spelling of `cache.max_bytes` (env: APERIO_CACHE_MAX_BYTES).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "minimum": 0
      },
      "cache_max_stale": {
        "description": "Deprecated spelling of `cache.max_stale` (env: APERIO_CACHE_MAX_STALE).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "minimum": 0
      },
      "cache_negative_ttl": {
        "description": "Flat spelling of `cache.negative_ttl` (env: APERIO_CACHE_NEGATIVE_TTL).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "examples": [
          10
        ],
        "minimum": 0
      },
      "client_down_threshold": {
        "description": "Seconds without a heartbeat before a client is considered down\n(env: APERIO_CLIENT_DOWN_THRESHOLD). Default: `15`.",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "examples": [
          15
        ],
        "minimum": 0
      },
      "config_hot_reload": {
        "description": "Watch the config file and apply live-editable changes without a restart\n(env: APERIO_CONFIG_HOT_RELOAD). Default: `true`.",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          true
        ]
      },
      "dashboard": {
        "description": "The built-in dashboard",
        "anyOf": [
          {
            "$ref": "#/$defs/DashboardSetting"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "examples": [
          true,
          {
            "enabled": true
          }
        ]
      },
      "data_dir": {
        "description": "Directory for the SQLite store and logs (env: APERIO_DATA_DIR).\nDefault: `./data`.",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "/app/data"
        ]
      },
      "db_max_bytes": {
        "description": "Cap on aperio.db (+WAL/SHM) in bytes; nearing it emits a warning, exceeding it auto-prunes low-priority data (env: APERIO_DB_MAX_BYTES).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "examples": [
          1073741824
        ],
        "minimum": 0
      },
      "denied_ips": {
        "description": "Source IPs/CIDRs refused everything, checked before every other rule:\nproxied traffic, the dashboard and its API, and the tunnel endpoints\nalike. The inverse of `allowed_ips`, for blocking an abusive address\nwithout turning on an allowlist that would lock out everyone unnamed.\nAnswers `403`. Hot-reloadable, so an address can be blocked without a\nrestart. An invalid entry refuses startup rather than applying a partial\ndeny list (env: APERIO_DENIED_IPS).",
        "type": [
          "array",
          "null"
        ],
        "examples": [
          [
            "203.0.113.7",
            "198.51.100.0/24"
          ]
        ],
        "items": {
          "type": "string"
        }
      },
      "edge": {
        "description": "Edge-proxy integration: publishing the served hostnames to a dynamic reverse proxy in front of this server",
        "anyOf": [
          {
            "$ref": "#/$defs/EdgeGroup"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "examples": [
          {
            "entrypoints": "websecure",
            "service_url": "http://aperio:8080",
            "token": "edge_xxxxxxxx"
          }
        ]
      },
      "edge_cert_resolver": {
        "description": "Deprecated spelling of `edge.cert_resolver` (env: APERIO_EDGE_CERT_RESOLVER).",
        "type": [
          "string",
          "null"
        ]
      },
      "edge_entrypoints": {
        "description": "Deprecated spelling of `edge.entrypoints` (env: APERIO_EDGE_ENTRYPOINTS).",
        "type": [
          "string",
          "null"
        ]
      },
      "edge_include_offline": {
        "description": "Deprecated spelling of `edge.include_offline` (env: APERIO_EDGE_INCLUDE_OFFLINE).",
        "type": [
          "boolean",
          "null"
        ]
      },
      "edge_service_url": {
        "description": "Deprecated spelling of `edge.service_url` (env: APERIO_EDGE_SERVICE_URL).",
        "type": [
          "string",
          "null"
        ]
      },
      "edge_token": {
        "description": "Deprecated spelling of `edge.token` (env: APERIO_EDGE_TOKEN).",
        "type": [
          "string",
          "null"
        ]
      },
      "error_pages": {
        "description": "Per-hostname custom 504/503 error pages (override the global\n`504_page`/`503_page` for that hostname).",
        "type": [
          "array",
          "null"
        ],
        "examples": [
          [
            {
              "504_page": "./pages/app-504.html",
              "hostname": "app.example.com"
            }
          ]
        ],
        "items": {
          "$ref": "#/$defs/ErrorPageRule"
        }
      },
      "expose": {
        "description": "Experimental public TCP expose ports.",
        "type": [
          "array",
          "null"
        ],
        "examples": [
          [
            {
              "port": 5432,
              "protocol": "tcp",
              "tunnel": "pg_main"
            }
          ]
        ],
        "items": {
          "$ref": "#/$defs/ExposeEntry"
        }
      },
      "failover": {
        "description": "In-flight failover: what happens to a request whose client disappears mid-flight",
        "anyOf": [
          {
            "$ref": "#/$defs/FailoverSetting"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "examples": [
          "retry-wait",
          {
            "max_jumps": 2,
            "mode": "retry",
            "window": 30
          }
        ]
      },
      "failover_all_methods": {
        "description": "Deprecated spelling of `failover.all_methods` (env: APERIO_FAILOVER_ALL_METHODS).",
        "type": [
          "boolean",
          "null"
        ]
      },
      "failover_max_jumps": {
        "description": "Deprecated spelling of `failover.max_jumps` (env: APERIO_FAILOVER_MAX_JUMPS).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint32",
        "minimum": 0
      },
      "failover_window": {
        "description": "Deprecated spelling of `failover.window` (env: APERIO_FAILOVER_WINDOW).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "minimum": 0
      },
      "fallbacks": {
        "description": "Per-hostname fallback URLs answered when no client serves the route.",
        "type": [
          "array",
          "null"
        ],
        "examples": [
          [
            {
              "hostname": "app.example.com",
              "preserve_path": true,
              "url": "https://status.example.com"
            }
          ]
        ],
        "items": {
          "$ref": "#/$defs/FallbackRule"
        }
      },
      "gateway": {
        "description": "Gateway timeouts applied to a proxied request",
        "anyOf": [
          {
            "$ref": "#/$defs/GatewayGroup"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "examples": [
          {
            "response_timeout": 30,
            "timeout": 10
          }
        ]
      },
      "gateway_response_timeout": {
        "description": "Deprecated spelling of `gateway.response_timeout` (env: APERIO_GATEWAY_RESPONSE_TIMEOUT).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "minimum": 0
      },
      "gateway_timeout": {
        "description": "Deprecated spelling of `gateway.timeout` (env: APERIO_GATEWAY_TIMEOUT).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "minimum": 0
      },
      "headers": {
        "description": "Server-wide request/response header rewrite rules applied to all traffic.",
        "anyOf": [
          {
            "$ref": "#/$defs/HeaderRules"
          },
          {
            "type": "null"
          }
        ],
        "examples": [
          {
            "request": {
              "add": {
                "X-Forwarded-Env": "staging"
              },
              "remove": [
                "X-Internal-Debug"
              ]
            },
            "response": {
              "add": {
                "X-Served-By": "aperio"
              },
              "remove": [
                "X-Powered-By"
              ]
            }
          }
        ]
      },
      "host": {
        "description": "Address to bind (bare env: HOST). Default: `0.0.0.0`.",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "0.0.0.0"
        ]
      },
      "identity_headers": {
        "description": "Tell the backend which client, organization and token served the\nrequest, as `x-aperio-client-id`, `x-aperio-org` and `x-aperio-token`.\nOff by default: they are new trust surface, and a backend that starts\nbelieving them should do so deliberately. Inbound `x-aperio-*` headers\nare stripped from every proxied request whatever this is set to, so a\nvisitor can never forge one (env: APERIO_IDENTITY_HEADERS).",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          true
        ]
      },
      "ignore_client_auth": {
        "description": "Ignore client-declared visitor passwords (env: APERIO_IGNORE_CLIENT_AUTH).\nDefault: `false`.",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          false
        ]
      },
      "inspector": {
        "description": "Record every proxied transaction for the dashboard's request inspector.\n`false` gives back a mutex, two header clones and a\ncapture entry per request, and nothing can be inspected or replayed\n(env: APERIO_INSPECTOR). Default: `true`.",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          false
        ]
      },
      "inspector_redact": {
        "description": "Mask credential headers and secret-looking body fields in the request\ninspector, the cURL copy and the HAR export\n(env: APERIO_INSPECTOR_REDACT). Default: `true`.",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          true
        ]
      },
      "ip_limit": {
        "description": "Per-visitor-IP rate limiting (token bucket)",
        "anyOf": [
          {
            "$ref": "#/$defs/IpLimitGroup"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "examples": [
          {
            "max": 120,
            "refill": 20
          }
        ]
      },
      "ip_limit_max": {
        "description": "Deprecated spelling of `ip_limit.max` (env: APERIO_IP_LIMIT_MAX).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "minimum": 0
      },
      "ip_limit_refill": {
        "description": "Deprecated spelling of `ip_limit.refill` (env: APERIO_IP_LIMIT_REFILL).",
        "type": [
          "number",
          "null"
        ],
        "format": "double"
      },
      "lb_strategy": {
        "description": "Load-balancing strategy (env: APERIO_LB_STRATEGY).\nDefault: `round-robin`.",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "round-robin",
          "primary-standby",
          "sticky"
        ]
      },
      "log_level": {
        "description": "Log level (bare env: LOG_LEVEL). Default: `info`.",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "info",
          "debug"
        ]
      },
      "login_lockout": {
        "description": "Dashboard login lockout after repeated failures",
        "anyOf": [
          {
            "$ref": "#/$defs/LoginLockoutGroup"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "examples": [
          {
            "secs": 60,
            "threshold": 5
          }
        ]
      },
      "login_lockout_secs": {
        "description": "Deprecated spelling of `login_lockout.secs` (env: APERIO_LOGIN_LOCKOUT_SECS).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "minimum": 0
      },
      "login_lockout_threshold": {
        "description": "Deprecated spelling of `login_lockout.threshold` (env: APERIO_LOGIN_LOCKOUT_THRESHOLD).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint32",
        "minimum": 0
      },
      "maintenance_windows": {
        "description": "Recurring maintenance windows, evaluated in their own time zone",
        "type": [
          "array",
          "null"
        ],
        "examples": [
          [
            {
              "days": [
                "sat"
              ],
              "from": "02:00",
              "hostname": "*.example.com",
              "to": "04:00",
              "tz": "Europe/Istanbul"
            }
          ]
        ],
        "items": {
          "$ref": "#/$defs/MaintenanceWindow"
        }
      },
      "max_body_size": {
        "description": "Largest request body in bytes (env: APERIO_MAX_BODY_SIZE).\nDefault: `10485760` (10 MB).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "examples": [
          10485760
        ],
        "minimum": 0
      },
      "max_concurrent_requests": {
        "description": "Concurrent proxied requests limit (env: APERIO_MAX_CONCURRENT_REQUESTS).\nDefault: `100`.",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "examples": [
          512
        ],
        "minimum": 0
      },
      "max_connections_per_service": {
        "description": "Parallel tunnel connections one client may open for a single service\n(its `connections:`). A token may lower this for its own holder, never\nraise it (env: APERIO_MAX_CONNECTIONS_PER_SERVICE). Default: `16`.",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "examples": [
          16
        ],
        "minimum": 0
      },
      "max_streams_per_ip": {
        "description": "Streamed responses one visitor address may hold open at once. `0` (the\ndefault) = no limit. Saturating a service's concurrency budget otherwise\ntakes one host holding many slow streams; this makes it take a botnet.\n\nNo default value is chosen for you on purpose: a NAT or a carrier-grade\nNAT puts many real people behind one address, so any number here is a\nguess with a queue of users behind it. Set it from what your own traffic\nlooks like, and make sure `trust_proxy` is right first, or every visitor\nbehind your CDN shares one address (env: APERIO_MAX_STREAMS_PER_IP).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint32",
        "examples": [
          8
        ],
        "minimum": 0
      },
      "max_tunnels": {
        "description": "Maximum simultaneously connected clients (env: APERIO_MAX_TUNNELS).\nDefault: `10`.",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "examples": [
          10
        ],
        "minimum": 0
      },
      "max_ws_connections": {
        "description": "Maximum concurrently-live proxied public WebSockets; they are long-lived,\nso they get their own ceiling separate from `max_concurrent_requests`.\n`0` = uncapped (env: APERIO_MAX_WS_CONNECTIONS). Default: `10000`.",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "examples": [
          1000
        ],
        "minimum": 0
      },
      "metrics": {
        "description": "The Prometheus metrics endpoint",
        "anyOf": [
          {
            "$ref": "#/$defs/MetricsSetting"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "examples": [
          true,
          {
            "enabled": true,
            "token": "scrape_xxxxxxxx"
          }
        ]
      },
      "metrics_token": {
        "description": "Deprecated spelling of `metrics.token` (env: APERIO_METRICS_TOKEN).",
        "type": [
          "string",
          "null"
        ]
      },
      "oidc": {
        "description": "OIDC single sign-on for the dashboard",
        "anyOf": [
          {
            "$ref": "#/$defs/OidcGroup"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "examples": [
          {
            "client_id": "aperio",
            "client_secret": "${OIDC_SECRET}",
            "issuer": "https://accounts.example.com",
            "redirect_url": "https://tunnel.example.com/aperio/oidc/callback"
          }
        ]
      },
      "oidc_allowed_emails": {
        "description": "Deprecated spelling of `oidc.allowed_emails` (env: APERIO_OIDC_ALLOWED_EMAILS).",
        "type": [
          "array",
          "null"
        ],
        "items": {
          "type": "string"
        }
      },
      "oidc_client_id": {
        "description": "Deprecated spelling of `oidc.client_id` (env: APERIO_OIDC_CLIENT_ID).",
        "type": [
          "string",
          "null"
        ]
      },
      "oidc_client_secret": {
        "description": "Deprecated spelling of `oidc.client_secret` (env: APERIO_OIDC_CLIENT_SECRET).",
        "type": [
          "string",
          "null"
        ]
      },
      "oidc_issuer": {
        "description": "Deprecated spelling of `oidc.issuer` (env: APERIO_OIDC_ISSUER).",
        "type": [
          "string",
          "null"
        ]
      },
      "oidc_redirect_url": {
        "description": "Deprecated spelling of `oidc.redirect_url` (env: APERIO_OIDC_REDIRECT_URL).",
        "type": [
          "string",
          "null"
        ]
      },
      "oidc_scopes": {
        "description": "Deprecated spelling of `oidc.scopes` (env: APERIO_OIDC_SCOPES).",
        "type": [
          "array",
          "null"
        ],
        "items": {
          "type": "string"
        }
      },
      "otel": {
        "description": "OpenTelemetry trace export",
        "anyOf": [
          {
            "$ref": "#/$defs/OtelSetting"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "examples": [
          true,
          {
            "enabled": true,
            "endpoint": "http://collector:4317",
            "protocol": "grpc",
            "service_name": "aperio"
          }
        ]
      },
      "otel_bridge": {
        "description": "Accept OpenTelemetry exports from tunnel clients and forward them to the\ncollector this server exports its own spans to (`otel.endpoint`). Off by\ndefault: it is an outbound path a client can drive, so it is a decision\nrather than a consequence of having `otel` on. While it is on, any client\nwith a valid tunnel token may export; every export is attributed to the\ntoken that sent it and both size and rate are capped\n(env: APERIO_OTEL_BRIDGE).",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          true
        ]
      },
      "otel_endpoint": {
        "description": "Deprecated spelling of `otel.endpoint` (env: APERIO_OTEL_ENDPOINT).",
        "type": [
          "string",
          "null"
        ]
      },
      "otel_headers": {
        "description": "Flat spelling of `otel.headers` (env: APERIO_OTEL_HEADERS).",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "authorization=Bearer xxx"
        ]
      },
      "otel_protocol": {
        "description": "Deprecated spelling of `otel.protocol` (env: APERIO_OTEL_PROTOCOL).",
        "type": [
          "string",
          "null"
        ]
      },
      "otel_sample_rate": {
        "description": "Deprecated spelling of `otel.sample_rate` (env: APERIO_OTEL_SAMPLE_RATE).",
        "type": [
          "number",
          "null"
        ],
        "format": "double"
      },
      "otel_service_name": {
        "description": "Deprecated spelling of `otel.service_name` (env: APERIO_OTEL_SERVICE_NAME).",
        "type": [
          "string",
          "null"
        ]
      },
      "outbound": {
        "description": "Where the server may send outbound callbacks (webhooks, autoscaling hooks)",
        "anyOf": [
          {
            "$ref": "#/$defs/OutboundGroup"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "examples": [
          {
            "allowlist": [
              "hooks.example.com",
              "*.provider.example"
            ],
            "block_private": true
          }
        ]
      },
      "outbound_allowlist": {
        "description": "Flat spelling of `outbound.allowlist` (env: APERIO_OUTBOUND_ALLOWLIST).",
        "type": [
          "array",
          "null"
        ],
        "examples": [
          [
            "hooks.example.com",
            "*.provider.example"
          ]
        ],
        "items": {
          "type": "string"
        }
      },
      "outbound_block_private": {
        "description": "Flat spelling of `outbound.block_private` (env: APERIO_OUTBOUND_BLOCK_PRIVATE).",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          true
        ]
      },
      "outlier_eject_secs": {
        "description": "Seconds an ejected client stays out before re-admission\n(env: APERIO_OUTLIER_EJECT_SECS). Default: `30`.",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "examples": [
          30
        ],
        "minimum": 0
      },
      "outlier_ejection": {
        "description": "Passive outlier ejection: temporarily drop a client from the pool when it\nreturns too many errors in a window (env: APERIO_OUTLIER_EJECTION).\nDefault: `false`.",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          true
        ]
      },
      "outlier_max_failures": {
        "description": "Failures inside the window before a client is ejected\n(env: APERIO_OUTLIER_MAX_FAILURES). Default: `5`.",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint32",
        "examples": [
          5
        ],
        "minimum": 0
      },
      "outlier_window": {
        "description": "Sliding window in seconds the failures are counted over\n(env: APERIO_OUTLIER_WINDOW). Default: `30`.",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "examples": [
          30
        ],
        "minimum": 0
      },
      "port": {
        "description": "Port to listen on (bare env: PORT). Default: `8080`.",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint16",
        "examples": [
          8080
        ],
        "maximum": 65535,
        "minimum": 0
      },
      "preview_noindex": {
        "description": "Inject noindex headers for random-subdomain preview services\n(env: APERIO_PREVIEW_NOINDEX). Default: `false`.",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          true
        ]
      },
      "random_subdomain": {
        "description": "Wildcard pattern granting each client a random subdomain (env: APERIO_RANDOM_SUBDOMAIN).",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "*.example.com"
        ]
      },
      "rate_limits": {
        "description": "Per-route request rate limits, capping aggregate rps to a host+path.",
        "type": [
          "array",
          "null"
        ],
        "examples": [
          [
            {
              "burst": 10,
              "hostname": "api.example.com",
              "path": "/api/login",
              "rps": 5
            }
          ]
        ],
        "items": {
          "$ref": "#/$defs/RateLimitRule"
        }
      },
      "real_ip_header": {
        "description": "Header carrying the real client IP (env: APERIO_REAL_IP_HEADER).",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "CF-Connecting-IP"
        ]
      },
      "request_id": {
        "description": "Request-id correlation: the id sent to the backend and echoed to the visitor",
        "anyOf": [
          {
            "$ref": "#/$defs/RequestIdGroup"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "examples": [
          {
            "enabled": true,
            "trust_inbound": false
          }
        ]
      },
      "request_id_header": {
        "description": "Flat spelling of `request_id.header` (env: APERIO_REQUEST_ID_HEADER).",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "x-correlation-id"
        ]
      },
      "request_id_trust_inbound": {
        "description": "Flat spelling of `request_id.trust_inbound`\n(env: APERIO_REQUEST_ID_TRUST_INBOUND).",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          true
        ]
      },
      "require_hostname_bind": {
        "description": "Require every client to carry a hostname bind\n(env: APERIO_REQUIRE_HOSTNAME_BIND). Default: `false`.",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          true
        ]
      },
      "retention": {
        "description": "How long each kind of recorded data is kept, in days",
        "anyOf": [
          {
            "$ref": "#/$defs/RetentionGroup"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "examples": [
          {
            "access_log": 30,
            "audit": 90,
            "captures": 7,
            "stats": 365
          }
        ]
      },
      "retention_access_log": {
        "description": "Deprecated spelling of `retention.access_log` (env: APERIO_RETENTION_ACCESS_LOG).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "minimum": 0
      },
      "retention_audit": {
        "description": "Deprecated spelling of `retention.audit` (env: APERIO_RETENTION_AUDIT).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "minimum": 0
      },
      "retention_captures": {
        "description": "Deprecated spelling of `retention.captures` (env: APERIO_RETENTION_CAPTURES).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "minimum": 0
      },
      "retention_stats": {
        "description": "Deprecated spelling of `retention.stats` (env: APERIO_RETENTION_STATS).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "minimum": 0
      },
      "retry_on_5xx": {
        "description": "Re-dispatch a buffered response whose status is a retryable server error\nto another client instead of returning it (env: APERIO_RETRY_ON_5XX).\nDefault: `false`.",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          true
        ]
      },
      "retry_statuses": {
        "description": "Status codes that trigger that retry; empty = every 5xx\n(env: APERIO_RETRY_STATUSES).",
        "type": [
          "array",
          "null"
        ],
        "examples": [
          [
            502,
            503
          ]
        ],
        "items": {
          "type": "integer",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0
        }
      },
      "reuseport": {
        "description": "Bind the listener with SO_REUSEPORT, so a second process can take over\nthe port for a zero-downtime handover (env: APERIO_REUSEPORT).\nDefault: `false`.",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          true
        ]
      },
      "routes": {
        "description": "Client-less routes: bind a hostname/path to a redirect or fixed response.",
        "type": [
          "array",
          "null"
        ],
        "examples": [
          [
            {
              "hostname": "old.example.com",
              "permanent": true,
              "redirect": "https://new.example.com"
            }
          ]
        ],
        "items": {
          "$ref": "#/$defs/RouteRule"
        }
      },
      "scaling": {
        "description": "Autoscaling: the server signalling desired capacity to an endpoint you control",
        "anyOf": [
          {
            "$ref": "#/$defs/ScalingSetting"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "examples": [
          true,
          {
            "allow_http": false,
            "enabled": true,
            "record_ttl": 2592000
          }
        ]
      },
      "scaling_allow_http": {
        "description": "Deprecated spelling of `scaling.allow_http` (env: APERIO_SCALING_ALLOW_HTTP).",
        "type": [
          "boolean",
          "null"
        ]
      },
      "scaling_allow_private": {
        "description": "Flat spelling of `scaling.allow_private` (env: APERIO_SCALING_ALLOW_PRIVATE).",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          false
        ]
      },
      "scaling_record_ttl": {
        "description": "Deprecated spelling of `scaling.record_ttl` (env: APERIO_SCALING_RECORD_TTL).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "minimum": 0
      },
      "secure_cookies": {
        "description": "Mark session cookies `Secure` (env: APERIO_SECURE_COOKIES).\nDefault: the `trust_proxy` value.",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          true
        ]
      },
      "server": {
        "description": "The server's own credentials",
        "anyOf": [
          {
            "$ref": "#/$defs/ServerCredentials"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "examples": [
          {
            "token": "change-me-to-a-long-random-string"
          }
        ]
      },
      "server_auth": {
        "description": "Deprecated spelling of `server.auth` (env: APERIO_SERVER_AUTH).",
        "type": [
          "string",
          "null"
        ]
      },
      "server_token": {
        "description": "Deprecated spelling of `server.token` (env: APERIO_SERVER_TOKEN).",
        "type": [
          "string",
          "null"
        ]
      },
      "shutdown_drain": {
        "description": "Seconds to let in-flight proxied requests finish before shutdown ends\nthe connections carrying them. Behind a load balancer this is the number\nthat decides whether a deploy is invisible or shows up as a handful of\n502s. `auto` sizes it from the drain budgets connected clients announce,\ncapped at 30 seconds. Default: `0` (do not wait)\n(env: APERIO_SHUTDOWN_DRAIN).",
        "anyOf": [
          {
            "$ref": "#/$defs/ShutdownDrain"
          },
          {
            "type": "null"
          }
        ],
        "examples": [
          10,
          "auto"
        ]
      },
      "shutdown_timeout": {
        "description": "Seconds after which shutdown stops waiting for anything still holding a\nconnection open (a proxied WebSocket, a TCP relay, a stalled peer) and\nexits. Default: `10` (env: APERIO_SHUTDOWN_TIMEOUT).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "examples": [
          30
        ],
        "minimum": 0
      },
      "stream": {
        "description": "Per-stream flow control for streamed data (responses, WebSocket, TCP)",
        "anyOf": [
          {
            "$ref": "#/$defs/StreamGroup"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "examples": [
          {
            "backlog_limit": 16777216,
            "pause_bytes": 2097152,
            "resume_bytes": 524288
          }
        ]
      },
      "stream_backlog_limit": {
        "description": "Flat spelling of `stream.backlog_limit` (env: APERIO_STREAM_BACKLOG_LIMIT).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "examples": [
          16777216
        ],
        "minimum": 0
      },
      "stream_min_throughput": {
        "description": "Flat spelling of `stream.min_throughput` (env: APERIO_STREAM_MIN_THROUGHPUT).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "examples": [
          1024
        ],
        "minimum": 0
      },
      "stream_pause_bytes": {
        "description": "Flat spelling of `stream.pause_bytes` (env: APERIO_STREAM_PAUSE_BYTES).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "examples": [
          2097152
        ],
        "minimum": 0
      },
      "stream_resume_bytes": {
        "description": "Flat spelling of `stream.resume_bytes` (env: APERIO_STREAM_RESUME_BYTES).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "examples": [
          524288
        ],
        "minimum": 0
      },
      "token_expiry_warning": {
        "description": "Seconds before a token's expiry to start warning\n(env: APERIO_TOKEN_EXPIRY_WARNING). Default: `86400` (24 hours).",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "examples": [
          7
        ],
        "minimum": 0
      },
      "token_pinning": {
        "description": "Pin a dynamic token to the first device key that presents it; a later\nconnection with a different (or missing) key is rejected\n(env: APERIO_TOKEN_PINNING). Default: `false`.",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          true
        ]
      },
      "trust_cf_header": {
        "description": "Trust the Cloudflare client-IP header (env: APERIO_TRUST_CF_HEADER).\nDefault: `false`.",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          true
        ]
      },
      "trust_proxy": {
        "description": "Trust `X-Forwarded-For` from proxies (env: APERIO_TRUST_PROXY).\nDefault: `false`.",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          true
        ]
      },
      "trusted_proxies": {
        "description": "Trusted proxy IPs/CIDRs (env: APERIO_TRUSTED_PROXIES).",
        "type": [
          "array",
          "null"
        ],
        "examples": [
          [
            "10.0.0.0/8"
          ]
        ],
        "items": {
          "type": "string"
        }
      },
      "tunnel_compression": {
        "description": "zlib-compress tunnel frames (env: APERIO_TUNNEL_COMPRESSION).\nDefault: `false`.",
        "type": [
          "boolean",
          "null"
        ],
        "examples": [
          true
        ]
      },
      "ui_language": {
        "description": "Default dashboard/login UI language (env: APERIO_UI_LANGUAGE).\nDefault: `en`.",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "en",
          "tr"
        ]
      },
      "uptime_tick_secs": {
        "description": "Seconds between availability-history ticks for the dashboard's Uptime\npanel, minimum 1 (env: APERIO_UPTIME_TICK_SECS). Default: `10`.",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "examples": [
          60
        ],
        "minimum": 0
      },
      "version": {
        "description": "The Aperio version this file was written for, e.g. `0.5.0`. On startup\nthe server compares it against its own build and reports every recorded\nchange to the configuration format that landed in between, refusing to\nstart when one of them has security consequences. Unset disables the\ncheck (env: APERIO_VERSION).",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "0.5.0"
        ]
      },
      "waf": {
        "description": "WAF-lite deny/size rules evaluated before a request is proxied.",
        "type": [
          "array",
          "null"
        ],
        "examples": [
          [
            {
              "path": "^/wp-admin"
            },
            {
              "header": {
                "name": "user-agent",
                "regex": "(?i)sqlmap|nikto"
              }
            },
            {
              "max_body": 1048576,
              "path": "^/upload"
            }
          ]
        ],
        "items": {
          "$ref": "#/$defs/WafRule"
        }
      },
      "webauthn_origin": {
        "description": "Public dashboard URL enabling passkeys; its domain is the RP ID (env: APERIO_WEBAUTHN_ORIGIN).",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "https://tunnel.example.com"
        ]
      },
      "webauthn_rp_id": {
        "description": "Passkey relying-party ID, when it must differ from the origin's domain\n(env: APERIO_WEBAUTHN_RP_ID).",
        "type": [
          "string",
          "null"
        ],
        "examples": [
          "example.com"
        ]
      },
      "webhook_retry_schedule": {
        "description": "Seconds between webhook delivery retries; empty = no retries\n(env: APERIO_WEBHOOK_RETRY_SCHEDULE). Default: `1,5,25,60`.",
        "type": [
          "array",
          "null"
        ],
        "examples": [
          [
            1,
            5,
            25,
            60
          ]
        ],
        "items": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        }
      }
    },
    "$defs": {
      "AlertGroup": {
        "description": "Alerting thresholds: when the server logs and emits an alert.\n\nWritten as a `alert:` block; the flat `alert_*` keys mean the same\nthing and still work, with the block winning per field.",
        "type": "object",
        "properties": {
          "client_down": {
            "description": "Seconds a known service may stay down before the client-down alert\nfires; it resolves when the service comes back. `0` = off. Default: off.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              60
            ],
            "minimum": 0
          },
          "error_rate": {
            "description": "Error-rate alert threshold, 0..1. Default: off.",
            "type": [
              "number",
              "null"
            ],
            "format": "double",
            "examples": [
              0.25
            ]
          },
          "min_requests": {
            "description": "Minimum requests in the window before the error-rate alert fires.\nDefault: `20`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              20
            ],
            "minimum": 0
          },
          "window": {
            "description": "Alert sliding-window seconds. Default: `300`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              300
            ],
            "minimum": 0
          }
        },
        "additionalProperties": false
      },
      "AlertRule": {
        "description": "One `alert_rules:` entry: a quantity the server measures, a bound, and how\nlong the condition must hold. A list of mappings, so it has no\nenvironment-variable equivalent.",
        "type": "object",
        "properties": {
          "above": {
            "description": "Fire while the value is strictly above this. Set this or `below`.",
            "type": [
              "number",
              "null"
            ],
            "format": "double",
            "examples": [
              536870912
            ]
          },
          "below": {
            "description": "Fire while the value is strictly below this. Set this or `above`.",
            "type": [
              "number",
              "null"
            ],
            "format": "double",
            "examples": [
              1
            ]
          },
          "for": {
            "description": "Seconds the condition must hold before firing, and hold clear before\nresolving. Default `0` = react on the first observation. Both directions\nuse it, so a value sitting on its threshold cannot alert every tick.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              300
            ],
            "minimum": 0
          },
          "metric": {
            "description": "What to watch: `connected_clients`, `pending_requests`, `store_bytes`\n(the SQLite store and its sidecars on disk), or `rss_bytes` (the server\nprocess's resident memory, Linux only).",
            "type": "string",
            "examples": [
              "store_bytes"
            ]
          },
          "name": {
            "description": "Names the alert. It becomes the event's `kind`, which is what a webhook\nreceiver switches on, so it has to be unique.",
            "type": "string",
            "examples": [
              "disk-filling"
            ]
          }
        },
        "additionalProperties": false,
        "required": [
          "name",
          "metric"
        ]
      },
      "AuditGroup": {
        "description": "Audit-log file rotation.\n\nWritten as a `audit:` block; the flat `audit_*` keys mean the same\nthing and still work, with the block winning per field.",
        "type": "object",
        "properties": {
          "max_files": {
            "description": "Rotated audit log files kept. Default: `3`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              3
            ],
            "minimum": 0
          },
          "max_size": {
            "description": "Audit log rotation size in bytes, 0 disables.\nDefault: `10485760` (10 MB).",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              10485760
            ],
            "minimum": 0
          }
        },
        "additionalProperties": false
      },
      "BackupGroup": {
        "description": "Scheduled snapshots of the SQLite store.\n\nWritten as a `backup:` block; the flat `backup_*` keys mean the same thing\nand still work, with the block winning per field.",
        "type": "object",
        "properties": {
          "dir": {
            "description": "Directory the snapshots are written to. Required to enable backups:\nwithout it (and a nonzero `interval`) no snapshots are taken.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "/var/backups/aperio"
            ]
          },
          "interval": {
            "description": "Seconds between snapshots; `0` disables scheduled backups. Default: off.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              86400
            ],
            "minimum": 0
          },
          "keep": {
            "description": "Snapshots to keep; older ones are pruned. Default: `7`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              7
            ],
            "minimum": 0
          }
        },
        "additionalProperties": false
      },
      "CacheGroup": {
        "description": "The server-side GET response cache.\n\nWritten as a `cache:` block; the flat `cache_*` keys mean the same\nthing and still work, with the block winning per field.",
        "type": "object",
        "properties": {
          "enabled": {
            "description": "Enable the server-side GET response cache. Default: `false`.",
            "type": [
              "boolean",
              "null"
            ],
            "examples": [
              true
            ]
          },
          "max_bytes": {
            "description": "Response-cache budget in bytes. Default: `67108864` (64 MB).",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              67108864
            ],
            "minimum": 0
          },
          "max_stale": {
            "description": "Serve-stale window in seconds for resilient services. Default: `3600`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              3600
            ],
            "minimum": 0
          },
          "negative_ttl": {
            "description": "Seconds to briefly cache error / negative responses (e.g. `404`), so a\nhot missing URL cannot hammer the backend. `0` = disabled.\nDefault: `0`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              10
            ],
            "minimum": 0
          }
        },
        "additionalProperties": false
      },
      "CacheSetting": {
        "description": "`cache:` written either as the bare value it has always accepted, or as\nthe block that carries its companion settings too. Default: `false`\n(disabled).",
        "anyOf": [
          {
            "description": "`true` turns the cache on with the defaults.",
            "type": "boolean"
          },
          {
            "description": "The full block.",
            "$ref": "#/$defs/CacheGroup"
          }
        ]
      },
      "DashboardGroup": {
        "description": "The built-in dashboard.\n\nWritten as a `dashboard:` block; the flat `dashboard_*` keys mean the same\nthing and still work, with the block winning per field.",
        "type": "object",
        "properties": {
          "enabled": {
            "description": "Serve the admin dashboard. Default: `true`.",
            "type": [
              "boolean",
              "null"
            ],
            "examples": [
              true
            ]
          }
        },
        "additionalProperties": false
      },
      "DashboardSetting": {
        "description": "`dashboard:` written either as the bare value it has always accepted, or as\nthe block that carries its companion settings too. Default: `true`\n(enabled).",
        "anyOf": [
          {
            "description": "`false` serves no dashboard at all.",
            "type": "boolean"
          },
          {
            "description": "The full block.",
            "$ref": "#/$defs/DashboardGroup"
          }
        ]
      },
      "EdgeGroup": {
        "description": "Edge-proxy integration: publishing the served hostnames to a dynamic reverse proxy in front of this server.\n\nWritten as a `edge:` block; the flat `edge_*` keys mean the same\nthing and still work, with the block winning per field.",
        "type": "object",
        "properties": {
          "cert_resolver": {
            "description": "Traefik certificate resolver for the generated routers.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "letsencrypt"
            ]
          },
          "entrypoints": {
            "description": "Traefik entry points for the generated routers.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "websecure"
            ]
          },
          "include_offline": {
            "description": "Also publish hostnames a token permits but no client serves yet.\nDefault: `false`.",
            "type": [
              "boolean",
              "null"
            ],
            "examples": [
              false
            ]
          },
          "service_url": {
            "description": "URL the edge proxy forwards matched traffic to.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "http://aperio:8080"
            ]
          },
          "token": {
            "description": "Credential enabling the edge-integration endpoints, which publish the\nlive hostname inventory to a reverse proxy in front of this server\n.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "change-me-to-a-long-random-string"
            ]
          }
        },
        "additionalProperties": false
      },
      "ErrorPageRule": {
        "description": "One `error_pages:` entry of `aperio-server.yaml`: per-hostname custom\n504/503 pages overriding the global `504_page`/`503_page` for that host.",
        "type": "object",
        "properties": {
          "503_page": {
            "description": "Path of the HTML file served on 503 maintenance responses.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "./pages/app-503.html"
            ]
          },
          "504_page": {
            "description": "Path of the HTML file served on 504 gateway-timeout responses.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "./pages/app-504.html"
            ]
          },
          "hostname": {
            "description": "Hostname the pages apply to (matched exactly, case-insensitive).",
            "type": "string",
            "examples": [
              "app.example.com"
            ]
          }
        },
        "required": [
          "hostname"
        ]
      },
      "ExposeEntry": {
        "description": "One `expose:` entry of `aperio-server.yaml`: a raw public TCP port the\nserver relays into a client's declared tunnel, with no binder peer.",
        "type": "object",
        "properties": {
          "key": {
            "description": "Deprecated spelling: a shared secret the client's tunnel declaration\nrepeats as `expose: <key>`. Still honored, but it names no owner, cannot\nbe revoked, and lives in plaintext in two files; prefer `tunnel` + `token`.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "k5fj2q-expose-secret"
            ]
          },
          "org": {
            "description": "Name of the organization whose client may claim this port. A tunnel name\nis unique inside an organization and nowhere else, so this is what makes\nthe claim unambiguous. Unset (with no `<org>@` prefix and no `token`)\nmeans the master organization.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "payments"
            ]
          },
          "port": {
            "description": "Public port the server listens on.",
            "type": "integer",
            "format": "uint16",
            "examples": [
              2222
            ],
            "maximum": 65535,
            "minimum": 0
          },
          "protocol": {
            "description": "Transport of the exposed port; only `tcp` is supported. A public UDP\nport is an amplification surface and is a separate decision.\nDefault: `tcp`.",
            "type": "string",
            "default": "tcp",
            "examples": [
              "tcp"
            ]
          },
          "token": {
            "description": "Name of the token whose client may claim this port. Superseded by `org`:\na token name is not unique across organizations, so a rule naming one\ncan match a client of another organization, and which one gets the port\nis not defined. Still honored; write `org` instead.",
            "type": [
              "string",
              "null"
            ],
            "deprecated": true,
            "examples": [
              "bastion-host"
            ]
          },
          "tunnel": {
            "description": "Name of the tunnel this port is relayed into. Preferred over `key`:\nthe claim is settled by identity (which organization declared the\ntunnel) rather than by a secret copied into two files. May be written\nas `<org>@<name>`, e.g. `payments@postgres`, which says the same thing\nas a separate `org:` and reads the way the dashboard shows it.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "ssh_bastion",
              "payments@postgres"
            ]
          }
        },
        "additionalProperties": false,
        "required": [
          "port"
        ]
      },
      "FailoverGroup": {
        "description": "In-flight failover: what happens to a request whose client disappears mid-flight.\n\nWritten as a `failover:` block; the flat `failover_*` keys mean the same\nthing and still work, with the block winning per field.",
        "type": "object",
        "properties": {
          "all_methods": {
            "description": "Allow failover for non-idempotent methods too. Default: `false`.",
            "type": [
              "boolean",
              "null"
            ],
            "examples": [
              false
            ]
          },
          "max_jumps": {
            "description": "Maximum failover re-dispatches per request. Default: `2`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint32",
            "examples": [
              2
            ],
            "minimum": 0
          },
          "mode": {
            "description": "In-flight failover mode. Default: `fail`.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "fail",
              "retry",
              "wait",
              "retry-wait"
            ]
          },
          "window": {
            "description": "Failover window in seconds. Default: `15`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              300
            ],
            "minimum": 0
          }
        },
        "additionalProperties": false
      },
      "FailoverSetting": {
        "description": "`failover:` written either as the bare value it has always accepted, or as\nthe block that carries its companion settings too. Default: `fail`.",
        "anyOf": [
          {
            "description": "The mode alone, e.g. `retry`.",
            "type": "string"
          },
          {
            "description": "The full block.",
            "$ref": "#/$defs/FailoverGroup"
          }
        ]
      },
      "FallbackRule": {
        "description": "One `fallbacks:` entry: where to send visitors of a hostname no client\ncurrently serves, instead of answering with a gateway error.",
        "type": "object",
        "properties": {
          "hostname": {
            "description": "Hostname to catch, or `*` for every unserved hostname.",
            "type": "string",
            "examples": [
              "app.example.com",
              "*"
            ]
          },
          "permanent": {
            "description": "Answer `308` instead of `307`, i.e. let clients cache the redirect.\nDefault: `false`.",
            "type": "boolean",
            "default": false
          },
          "preserve_path": {
            "description": "Append the requested path to the target URL. Default: `false`.",
            "type": "boolean",
            "default": false
          },
          "url": {
            "description": "URL visitors are redirected to.",
            "type": "string",
            "examples": [
              "https://status.example.com"
            ]
          }
        },
        "additionalProperties": false,
        "required": [
          "hostname",
          "url"
        ]
      },
      "GatewayGroup": {
        "description": "Gateway timeouts applied to a proxied request.\n\nWritten as a `gateway:` block; the flat `gateway_*` keys mean the same\nthing and still work, with the block winning per field.",
        "type": "object",
        "properties": {
          "response_timeout": {
            "description": "Seconds to wait for a client response. Default: `30`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              30
            ],
            "minimum": 0
          },
          "timeout": {
            "description": "Seconds to wait for a client connection. Default: `10`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              10
            ],
            "minimum": 0
          }
        },
        "additionalProperties": false
      },
      "HeaderDirectives": {
        "description": "Header edits applied to one direction of proxied traffic (request or\nresponse): `add` sets headers (replacing any existing value of the same\nname), `remove` strips headers by name (case-insensitive).",
        "type": "object",
        "properties": {
          "add": {
            "description": "Headers to set, name → value; replaces an existing header of the same name.",
            "type": "object",
            "additionalProperties": {
              "type": "string"
            },
            "default": {},
            "examples": [
              {
                "X-Forwarded-Env": "staging"
              }
            ]
          },
          "remove": {
            "description": "Header names to strip (case-insensitive).",
            "type": "array",
            "default": [],
            "examples": [
              [
                "Server",
                "X-Powered-By"
              ]
            ],
            "items": {
              "type": "string"
            }
          }
        }
      },
      "HeaderRules": {
        "description": "Header add/remove rules for proxied HTTP traffic: `request` edits what the\nlocal backend receives, `response` edits what the visitor receives.\nHop-by-hop and tunnel-critical headers stay managed by Aperio regardless.",
        "type": "object",
        "properties": {
          "request": {
            "description": "Edits applied to forwarded requests before they reach the local backend.",
            "anyOf": [
              {
                "$ref": "#/$defs/HeaderDirectives"
              },
              {
                "type": "null"
              }
            ],
            "examples": [
              {
                "add": {
                  "X-Forwarded-Env": "staging"
                },
                "remove": [
                  "X-Internal-Debug"
                ]
              }
            ]
          },
          "response": {
            "description": "Edits applied to backend responses before they return to the visitor.",
            "anyOf": [
              {
                "$ref": "#/$defs/HeaderDirectives"
              },
              {
                "type": "null"
              }
            ],
            "examples": [
              {
                "add": {
                  "X-Served-By": "aperio"
                },
                "remove": [
                  "X-Powered-By"
                ]
              }
            ]
          }
        }
      },
      "IpLimitGroup": {
        "description": "Per-visitor-IP rate limiting (token bucket).\n\nWritten as a `ip_limit:` block; the flat `ip_limit_*` keys mean the same\nthing and still work, with the block winning per field.",
        "type": "object",
        "properties": {
          "max": {
            "description": "Per-IP rate-limit burst. Default: `100`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              120
            ],
            "minimum": 0
          },
          "refill": {
            "description": "Per-IP rate-limit refill per second. Default: `5`.",
            "type": [
              "number",
              "null"
            ],
            "format": "double",
            "examples": [
              20
            ]
          }
        },
        "additionalProperties": false
      },
      "LoginLockoutGroup": {
        "description": "Dashboard login lockout after repeated failures.\n\nWritten as a `login_lockout:` block; the flat `login_lockout_*` keys mean the same\nthing and still work, with the block winning per field.",
        "type": "object",
        "properties": {
          "secs": {
            "description": "Base lockout seconds, doubled per repeat. Default: `60`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              60
            ],
            "minimum": 0
          },
          "threshold": {
            "description": "Failed logins per IP before a lockout. Default: `5`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint32",
            "examples": [
              5
            ],
            "minimum": 0
          }
        },
        "additionalProperties": false
      },
      "MaintenanceWindow": {
        "description": "One `maintenance_windows:` entry: a recurring window during which matching\nhostnames answer the maintenance page by themselves.\n\nA list of mappings, so it has no environment-variable equivalent, like\n`routes:` and `rate_limits:`.",
        "type": "object",
        "properties": {
          "days": {
            "description": "Weekdays the window runs on (`mon`..`sun`, long names accepted).\nOmitted = every day.",
            "type": [
              "array",
              "null"
            ],
            "examples": [
              [
                "sat",
                "sun"
              ]
            ],
            "items": {
              "type": "string"
            }
          },
          "from": {
            "description": "Local start time, `HH:MM`.",
            "type": "string",
            "examples": [
              "02:00"
            ]
          },
          "hostname": {
            "description": "Hostname or pattern the window applies to, in the same shapes the\nmaintenance API takes (`app.example.com`, `*.example.com`,\n`*-pi.example.com`). Omitted or `*` = every hostname on the server.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "*.example.com"
            ]
          },
          "reason": {
            "description": "Shown on the maintenance page and in the dashboard while the window is\nrunning.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "weekly patching"
            ]
          },
          "to": {
            "description": "Local end time, `HH:MM`, exclusive. Earlier than `from` means the window\nwraps past midnight, and `days` then names the day it *starts*.",
            "type": "string",
            "examples": [
              "04:00"
            ]
          },
          "tz": {
            "description": "IANA time zone the times are local to. Default: `UTC`. Use a named zone\nrather than a fixed offset so the window stays put across a\ndaylight-saving change.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "Europe/Istanbul"
            ]
          }
        },
        "additionalProperties": false,
        "required": [
          "from",
          "to"
        ]
      },
      "MetricsGroup": {
        "description": "The Prometheus metrics endpoint.\n\nWritten as a `metrics:` block; the flat `metrics_*` keys mean the same\nthing and still work, with the block winning per field.",
        "type": "object",
        "properties": {
          "enabled": {
            "description": "Prometheus metrics endpoint toggle. Default: `false`.",
            "type": [
              "boolean",
              "null"
            ],
            "examples": [
              true
            ]
          },
          "token": {
            "description": "Bearer token gating the metrics endpoint.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "change-me-to-a-long-random-string"
            ]
          }
        },
        "additionalProperties": false
      },
      "MetricsSetting": {
        "description": "`metrics:` written either as the bare value it has always accepted, or as\nthe block that carries its companion settings too. Default: `false`\n(disabled).",
        "anyOf": [
          {
            "description": "`true` exposes the Prometheus endpoint.",
            "type": "boolean"
          },
          {
            "description": "The full block.",
            "$ref": "#/$defs/MetricsGroup"
          }
        ]
      },
      "OidcGroup": {
        "description": "OIDC single sign-on for the dashboard.\n\nWritten as a `oidc:` block; the flat `oidc_*` keys mean the same\nthing and still work, with the block winning per field.",
        "type": "object",
        "properties": {
          "allowed_emails": {
            "description": "Allowed OIDC login emails.",
            "type": [
              "array",
              "null"
            ],
            "examples": [
              [
                "alice@example.com",
                "ops@example.com"
              ]
            ],
            "items": {
              "type": "string"
            }
          },
          "client_id": {
            "description": "OIDC client id.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "aperio"
            ]
          },
          "client_secret": {
            "description": "OIDC client secret.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "${OIDC_SECRET}"
            ]
          },
          "issuer": {
            "description": "OIDC issuer URL.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "https://accounts.example.com"
            ]
          },
          "redirect_url": {
            "description": "OIDC redirect URL override.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "https://tunnel.example.com/aperio/oidc/callback"
            ]
          },
          "scopes": {
            "description": "OIDC scopes. Default: `openid email profile`.",
            "type": [
              "array",
              "null"
            ],
            "examples": [
              [
                "openid",
                "email",
                "profile"
              ]
            ],
            "items": {
              "type": "string"
            }
          }
        },
        "additionalProperties": false
      },
      "OtelGroup": {
        "description": "OpenTelemetry trace export.\n\nWritten as a `otel:` block; the flat `otel_*` keys mean the same\nthing and still work, with the block winning per field.",
        "type": "object",
        "properties": {
          "enabled": {
            "description": "Enable OpenTelemetry OTLP export. Default: `false`.",
            "type": [
              "boolean",
              "null"
            ],
            "examples": [
              true
            ]
          },
          "endpoint": {
            "description": "OTLP endpoint. The conventional ports are 4318 for OTLP/HTTP and 4317\nfor OTLP/gRPC; `protocol` follows from the port unless it is set.\nDefault: `http://localhost:4318`.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "http://localhost:4318",
              "http://localhost:4317"
            ]
          },
          "headers": {
            "description": "Extra headers for every outgoing OTLP request, as `k=v,k=v`. This is\nwhere a collector's credential goes, and it stays on the server: the\npoint of the OTel bridge is that an edge host does not hold one.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "authorization=Bearer xxx"
            ]
          },
          "protocol": {
            "description": "OTLP transport: `http` (protobuf over HTTP) or `grpc`. Unset picks `grpc`\nfor an endpoint on port 4317 and `http` everywhere else, a collector\nanswering the wrong protocol drops every span silently, so pin this when\nthe endpoint runs on a non-standard port.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "http",
              "grpc"
            ]
          },
          "sample_rate": {
            "description": "Fraction of traces to record, 0.0 to 1.0. At `1.0` every request\nbuilds a span tree and hands it to the exporter, which is the setting\nthat makes tracing show up in a benchmark. `0.01` samples one request in\na hundred, which answers the same questions about latency and error\nshape for a hundredth of the cost. The decision is made once per request\nand every span of that request follows it. Default: `1.0`.",
            "type": [
              "number",
              "null"
            ],
            "format": "double",
            "examples": [
              0.01
            ]
          },
          "service_name": {
            "description": "OTLP service name. Default: `aperio-server`.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "aperio"
            ]
          }
        },
        "additionalProperties": false
      },
      "OtelSetting": {
        "description": "`otel:` written either as the bare value it has always accepted, or as\nthe block that carries its companion settings too. Default: `false`\n(disabled).",
        "anyOf": [
          {
            "description": "`true` exports traces with the defaults.",
            "type": "boolean"
          },
          {
            "description": "The full block.",
            "$ref": "#/$defs/OtelGroup"
          }
        ]
      },
      "OutboundGroup": {
        "description": "Where the server may send outbound callbacks (webhook deliveries,\nautoscaling hooks). Optional: empty/off keeps the permissive default.\n\nWritten as an `outbound:` block; the flat `outbound_*` keys mean the\nsame thing and still work, with the block winning per field.",
        "type": "object",
        "properties": {
          "allowlist": {
            "description": "Host/CIDR patterns the server may call (exact host, `*.suffix`, IP, or\nCIDR). When set, any other destination is refused; a matching entry is\ntrusted even if private.",
            "type": [
              "array",
              "null"
            ],
            "examples": [
              [
                "hooks.example.com",
                "*.provider.example"
              ]
            ],
            "items": {
              "type": "string"
            }
          },
          "block_private": {
            "description": "With no allowlist: refuse destinations resolving to internal addresses\n(loopback, RFC 1918, link-local/metadata, CGNAT, unique-local).\nDefault: `false`.",
            "type": [
              "boolean",
              "null"
            ],
            "examples": [
              true
            ]
          }
        },
        "additionalProperties": false
      },
      "RateLimitRule": {
        "description": "One `rate_limits:` entry: an aggregate requests-per-second ceiling for a\nhostname and/or path prefix, independent of the per-visitor IP limit.",
        "type": "object",
        "properties": {
          "burst": {
            "description": "Burst capacity. Default: the `rps` value (one second's worth).",
            "type": [
              "number",
              "null"
            ],
            "format": "double",
            "examples": [
              100
            ]
          },
          "hostname": {
            "description": "Hostname the rule applies to (omit for any host).",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "api.example.com"
            ]
          },
          "methods": {
            "description": "HTTP methods the rule applies to (omit for every method). Lets a write\npath be limited without throttling reads of the same route.",
            "type": [
              "array",
              "null"
            ],
            "examples": [
              [
                "POST",
                "PUT",
                "DELETE"
              ]
            ],
            "items": {
              "type": "string"
            }
          },
          "path": {
            "description": "Path prefix the rule applies to (omit for any path).",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "/api"
            ]
          },
          "rps": {
            "description": "Sustained requests per second allowed to the route.",
            "type": "number",
            "format": "double",
            "examples": [
              50
            ]
          }
        },
        "additionalProperties": false,
        "required": [
          "rps"
        ]
      },
      "RequestIdGroup": {
        "description": "Request-id correlation (`request_id:`): the id the server already assigns\nevery proxied request, made visible to the backend and to the visitor.",
        "type": "object",
        "properties": {
          "enabled": {
            "description": "Send the id to the backend and echo it on the response. On by default\n(env: APERIO_REQUEST_ID).",
            "type": [
              "boolean",
              "null"
            ],
            "examples": [
              false
            ]
          },
          "header": {
            "description": "Header carrying it. Default: `x-request-id`\n(env: APERIO_REQUEST_ID_HEADER).",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "x-correlation-id"
            ]
          },
          "trust_inbound": {
            "description": "Adopt the visitor's own value when the request already carries one,\ninstead of ignoring it. Off by default: the header is attacker-supplied,\nso trusting it lets a visitor choose what appears in your logs and in\nyour backend's. Turn it on behind a proxy that sets the header itself.\nDefault: `false` (env: APERIO_REQUEST_ID_TRUST_INBOUND).",
            "type": [
              "boolean",
              "null"
            ],
            "examples": [
              true
            ]
          }
        },
        "additionalProperties": false
      },
      "RespondRule": {
        "description": "The fixed response of a client-less `respond` route.",
        "type": "object",
        "properties": {
          "body": {
            "description": "Response body.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "<h1>Coming soon</h1>"
            ]
          },
          "content_type": {
            "description": "`Content-Type` of the response body.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "text/html; charset=utf-8"
            ]
          },
          "status": {
            "description": "HTTP status to answer with. Default: `200`.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint16",
            "examples": [
              503
            ],
            "maximum": 65535,
            "minimum": 0
          }
        }
      },
      "RetentionGroup": {
        "description": "How long each kind of recorded data is kept, in days.\n\nWritten as a `retention:` block; the flat `retention_*` keys mean the same\nthing and still work, with the block winning per field.",
        "type": "object",
        "properties": {
          "access_log": {
            "description": "Days to keep access-log file lines; `0` = forever. Default: off (keep).",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              30
            ],
            "minimum": 0
          },
          "audit": {
            "description": "Days to keep audit events; `0` = forever. Default: off (keep).",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              90
            ],
            "minimum": 0
          },
          "captures": {
            "description": "Days to keep inspector captures and webhook inbox entries; `0` =\nforever. Default: off (keep).",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              7
            ],
            "minimum": 0
          },
          "stats": {
            "description": "Days to keep day-granularity stats buckets; `0` = the built-in caps.\nDefault: off (built-in caps).",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              365
            ],
            "minimum": 0
          }
        },
        "additionalProperties": false
      },
      "RouteCanary": {
        "description": "A `canary:` block inside a `routes:` entry: which service gets the new\nversion's traffic, how much of it, and how somebody opts in by hand.\n\nWeighted routing and a header-based canary are the same mechanism seen from\ntwo angles, which is why they are one block rather than two settings.",
        "type": "object",
        "properties": {
          "header": {
            "description": "Request header that sends this visitor to the canary whatever the weight\nsays, so a developer can reach the new version on demand.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "x-canary"
            ]
          },
          "service": {
            "description": "Name of the `services:` entry serving the new version. Clients that\nannounce this service name are the canary side; every other client\nserving the route is the stable side.",
            "type": "string",
            "examples": [
              "web-v2"
            ]
          },
          "value": {
            "description": "Value that header must carry. Unset = any non-empty value.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "on"
            ]
          },
          "weight": {
            "description": "Percentage of visitors sent there without asking, `0` to `100`. Default:\n`0`, which with a `header` set is the opt-in-only shape.\n\nThe split is decided per **visitor**, by hashing their address, not per\nrequest: a per-request coin flip would send one page load's twenty assets\nto both versions, which is a mixture rather than a canary and breaks the\nthing being tested first. The cost is that the split is only as even as\nthe addresses are spread, so at low traffic or behind one large NAT\ntwenty percent may not look like twenty percent.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint8",
            "examples": [
              20
            ],
            "maximum": 255,
            "minimum": 0
          }
        },
        "additionalProperties": false,
        "required": [
          "service"
        ]
      },
      "RouteRateLimit": {
        "description": "A `rate_limit:` block inside a `routes:` entry: the same token bucket as a\n`rate_limits:` rule, without repeating the hostname and path.",
        "type": "object",
        "properties": {
          "burst": {
            "description": "Burst capacity. Default: the `rps` value (one second's worth).",
            "type": [
              "number",
              "null"
            ],
            "format": "double",
            "examples": [
              100
            ]
          },
          "methods": {
            "description": "HTTP methods the limit applies to (omit for every method). Lets a write\npath be limited without throttling reads of the same route.",
            "type": [
              "array",
              "null"
            ],
            "examples": [
              [
                "POST",
                "PUT",
                "DELETE"
              ]
            ],
            "items": {
              "type": "string"
            }
          },
          "rps": {
            "description": "Sustained requests per second allowed to the route.",
            "type": "number",
            "format": "double",
            "examples": [
              50
            ]
          }
        },
        "additionalProperties": false,
        "required": [
          "rps"
        ]
      },
      "RouteRule": {
        "description": "One `routes:` entry of `aperio-server.yaml`. Either an *answer* rule, a\nhostname/path match paired with exactly one action (`redirect` or\n`respond`) served without a client, or a *policy* rule, which has neither\naction and instead carries settings (`timeout`, `headers`, `rate_limit`)\nthat apply to proxied traffic matching it. The two kinds are matched\nindependently, each first-match in file order.",
        "type": "object",
        "properties": {
          "canary": {
            "description": "Split this route's traffic between two versions of a service. Policy\nfield.",
            "anyOf": [
              {
                "$ref": "#/$defs/RouteCanary"
              },
              {
                "type": "null"
              }
            ],
            "examples": [
              {
                "header": "x-canary",
                "service": "web-v2",
                "weight": 20
              }
            ]
          },
          "headers": {
            "description": "Header edits for this route only, applied after the server-wide\n`headers:` rules so a route can override them. Policy field.",
            "anyOf": [
              {
                "$ref": "#/$defs/HeaderRules"
              },
              {
                "type": "null"
              }
            ],
            "examples": [
              {
                "response": {
                  "add": {
                    "cache-control": "public, max-age=3600"
                  }
                }
              }
            ]
          },
          "hostname": {
            "description": "Hostname to match exactly (unset = any hostname).",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "old.example.com"
            ]
          },
          "path": {
            "description": "Path prefix to match, with bind semantics (unset = any path).",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "/robots.txt"
            ]
          },
          "permanent": {
            "description": "Use a permanent 301 instead of the default 302. Default: `false`.",
            "type": "boolean",
            "default": false
          },
          "preserve_path": {
            "description": "Append the request's path and query to the redirect target.\nDefault: `false`.",
            "type": "boolean",
            "default": false
          },
          "rate_limit": {
            "description": "Rate limit for this route only, so the hostname and path are written\nonce instead of repeated under `rate_limits:`. Wins over any\n`rate_limits:` entry matching the same request. Policy field.",
            "anyOf": [
              {
                "$ref": "#/$defs/RouteRateLimit"
              },
              {
                "type": "null"
              }
            ],
            "examples": [
              {
                "burst": 20,
                "methods": [
                  "POST"
                ],
                "rps": 10
              }
            ]
          },
          "redirect": {
            "description": "Redirect target; answers 302 (or 301 with `permanent: true`).",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "https://new.example.com"
            ]
          },
          "respond": {
            "description": "Serve a fixed response instead of redirecting.",
            "anyOf": [
              {
                "$ref": "#/$defs/RespondRule"
              },
              {
                "type": "null"
              }
            ],
            "examples": [
              {
                "body": "Be right back",
                "content_type": "text/plain",
                "status": 503
              }
            ]
          },
          "timeout": {
            "description": "Seconds to wait for the serving client's answer on this route, overriding\n`gateway.response_timeout` and any per-service `response_timeout` the\nclient declared. Policy field: only valid on an entry that has neither\n`redirect` nor `respond`, since those never reach a backend.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              120
            ],
            "minimum": 0
          }
        }
      },
      "ScalingGroup": {
        "description": "Autoscaling: the server signalling desired capacity to an endpoint you control.\n\nWritten as a `scaling:` block; the flat `scaling_*` keys mean the same\nthing and still work, with the block winning per field.",
        "type": "object",
        "properties": {
          "allow_http": {
            "description": "Allow a plain-http autoscaling endpoint. Default: `false`.",
            "type": [
              "boolean",
              "null"
            ],
            "examples": [
              false
            ]
          },
          "allow_private": {
            "description": "Allow an autoscaling endpoint on a private or loopback address, for a\nprovider API that genuinely lives on the internal network.\nDefault: `false`.",
            "type": [
              "boolean",
              "null"
            ],
            "examples": [
              false
            ]
          },
          "enabled": {
            "description": "Honor client `scaling:` declarations. Default: `false`.",
            "type": [
              "boolean",
              "null"
            ],
            "examples": [
              true
            ]
          },
          "record_ttl": {
            "description": "Seconds after which an unrefreshed autoscaling record is dropped.\nDefault: `2592000` (30 days).",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              2592000
            ],
            "minimum": 0
          }
        },
        "additionalProperties": false
      },
      "ScalingSetting": {
        "description": "`scaling:` written either as the bare value it has always accepted, or as\nthe block that carries its companion settings too. Default: `false`\n(disabled).",
        "anyOf": [
          {
            "description": "`true` honors the clients' scaling declarations.",
            "type": "boolean"
          },
          {
            "description": "The full block.",
            "$ref": "#/$defs/ScalingGroup"
          }
        ]
      },
      "ServerCredentials": {
        "description": "The server's own credentials.\n\nWritten as a `server:` block; the flat `server_*` keys mean the same\nthing and still work, with the block winning per field.",
        "type": "object",
        "properties": {
          "auth": {
            "description": "Visitor auth `user:password` gate.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "admin:s3cret"
            ]
          },
          "token": {
            "description": "Master token; also the fallback dashboard password.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "change-me-to-a-long-random-string"
            ]
          }
        },
        "additionalProperties": false
      },
      "ShutdownDrain": {
        "description": "`shutdown_drain:` as a number of seconds, or the word `auto`.",
        "anyOf": [
          {
            "description": "Wait this many seconds for in-flight requests.",
            "type": "integer",
            "format": "uint64",
            "minimum": 0
          },
          {
            "description": "`auto`: take the longest drain budget connected clients announce,\ncapped, so the number follows the deployment instead of being a constant\nnobody revisits.",
            "type": "string"
          }
        ]
      },
      "StreamGroup": {
        "description": "Per-stream flow control for streamed data (responses, WebSocket, TCP).\n\nWritten as a `stream:` block; the flat `stream_*` keys mean the same\nthing and still work, with the block winning per field.",
        "type": "object",
        "properties": {
          "backlog_limit": {
            "description": "Hard per-stream backlog cap in bytes (drops producers that cannot pause).\nDefault: `16777216` (16 MB).",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              16777216
            ],
            "minimum": 0
          },
          "min_throughput": {
            "description": "Bytes per second a streamed response's consumer must take **while data\nis waiting for it**, or the stream is ended. `0` (the default) = no\nfloor.\n\nThe pump already ends a stream whose consumer cannot take a single chunk\nwithin the gateway timeout, so a reader that takes nothing is covered.\nThis closes the gap in between: a reader that accepts one chunk just\ninside the timeout, forever, holding a client concurrency slot and\nmegabytes of buffer for as long as it likes.\n\nOnly time the consumer kept data waiting counts, so a stream that is\nquiet because the *backend* has nothing to send, which is ordinary for\nserver-sent events and long polling, is never ended for it\n(env: APERIO_STREAM_MIN_THROUGHPUT).",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              1024
            ],
            "minimum": 0
          },
          "pause_bytes": {
            "description": "Backlog bytes at which the producing client is asked to pause a stream.\nDefault: `2097152` (2 MB).",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              2097152
            ],
            "minimum": 0
          },
          "resume_bytes": {
            "description": "Backlog bytes under which a paused producer is asked to resume.\nDefault: `524288` (512 KB).",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint64",
            "examples": [
              524288
            ],
            "minimum": 0
          }
        },
        "additionalProperties": false
      },
      "WafHeaderMatch": {
        "description": "A header match inside a `waf:` rule.",
        "type": "object",
        "properties": {
          "name": {
            "description": "Header name to inspect (case-insensitive).",
            "type": "string",
            "examples": [
              "user-agent"
            ]
          },
          "regex": {
            "description": "Regular expression the header value must match for the rule to fire.",
            "type": "string",
            "examples": [
              "(?i)sqlmap|nikto"
            ]
          }
        },
        "additionalProperties": false,
        "required": [
          "name",
          "regex"
        ]
      },
      "WafRule": {
        "description": "One `waf:` entry: a request is denied with `403` when every set condition\nmatches, or answered `413` when `max_body` is exceeded.",
        "type": "object",
        "properties": {
          "header": {
            "description": "Header condition the request must match.",
            "anyOf": [
              {
                "$ref": "#/$defs/WafHeaderMatch"
              },
              {
                "type": "null"
              }
            ],
            "examples": [
              {
                "name": "user-agent",
                "regex": "(?i)sqlmap|nikto"
              }
            ]
          },
          "max_body": {
            "description": "Body-size ceiling in bytes for the matched route; makes this a `413`\nsize rule rather than a `403` deny rule.",
            "type": [
              "integer",
              "null"
            ],
            "format": "uint",
            "examples": [
              1048576
            ],
            "minimum": 0
          },
          "methods": {
            "description": "HTTP methods the rule applies to (omit for any).",
            "type": [
              "array",
              "null"
            ],
            "examples": [
              [
                "POST",
                "PUT"
              ]
            ],
            "items": {
              "type": "string"
            }
          },
          "path": {
            "description": "Regular expression matched against the request path.",
            "type": [
              "string",
              "null"
            ],
            "examples": [
              "^/wp-admin"
            ]
          }
        },
        "additionalProperties": false
      }
    }
  }
}
