# The same vintage, plus the one key whose removal is recorded as `Security`:
# the file claims the dashboard is behind its own password, and it is not.
# A `Security` entry must refuse the start rather than print a notice, and
# that is the half of the mechanism nothing exercised (planned_features #91).
version: 0.5.0

server_token: e2e-upgrade-fixture-token-long-enough
dashboard_auth: "admin:leftover"
