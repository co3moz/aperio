server:
  url: https://tunnel.example.com
  token: apr_dashboard_change_me

services:
  # A plain service, so the dashboard has live traffic to show.
  - target: http://localhost:3000
    hostname: app.example.com
