server_token: apr_health_check_change_me
port: 8080

# Optional: fire an `alert_triggered` webhook/audit event (kind `client_down`)
# when a known service stays down for this many seconds; resolves on recovery.
alert_client_down: 60
