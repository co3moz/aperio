server_token: apr_allowed_ips_multi_change_me
port: 8080

# Behind a reverse proxy, resolve real visitor IPs or the allowlist is
# meaningless (every visitor would appear as the proxy).
# trusted_proxies:
#   - 10.0.0.0/8
